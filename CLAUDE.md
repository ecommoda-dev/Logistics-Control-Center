<div dir="rtl" style="text-align: right;">

# Logistics-Control-Center — سياق الأداة

![version](https://img.shields.io/badge/version-v1.12.1-blue)

> الملف ده بيتحمّل تلقائيًا في أي جلسة على الريبو ده. القواعد المشتركة في السكيلز —
> هنا سياق الأداة دي بس.

---

## إيه هي

داشبورد **قراءة فقط** لمسئول الشحن: حالات الأوردرات إحصائيًا وتفصيليًا من
**01-05-2026** (بداية تشغيل المتجر) وحتى النهارده، مع المناديب والمناطق
والمدن، طابور التجهيز (الطباعة والتغليف)، مواعيد الاستلام، أسباب الإلغاء
والمرتجع، والملاحظات.

**مفيش أي mutation على شوبيفاي في الأداة دي.** أي طلب "خلّيها تعدّل حالة" =
أداة تانية، مش تعديل هنا.

**مفيش أي بيانات قبل 01-05-2026** — راجع "🔴 قرار تاريخ بداية البيانات" تحت.

---

## الهوية والثوابت

| | |
|---|---|
| الريبو | `ecommoda-dev/Logistics-Control-Center` |
| Worker | `logistics-control-center-worker` |
| الواجهة | `https://ecommoda-dev.github.io/Logistics-Control-Center/` |
| `TOOL_NAME` (D1) | `logistics_control_center` |
| قيم `type` | `login` · `logout` **فقط** |
| `TOOL_VERSION` (HTML) | `v1.12.2` |
| `WORKER_VERSION` | `v1.1.2` |
| `CACHE_VERSION` | `v1` |
| `STORE_START_DATE` | `2026-05-01` — معرّفة في `index.js` **و** `index.html`، لازم تتطابق |
| ربط D1 | `DB` → `ecommoda-dev-logs` |
| ربط KV | `DASH_KV` → namespace خاص بالأداة |
| CORS | Option A (wildcard) — أداة قراءة فقط |
| Container Tier | **L** (`1400px`) — داشبورد بمربعات + رسوم + جدول كثيف |
| تسجيل الدخول | 🔒 **مطلوب** — Universal D1 Auth (قرار أحمد 29-08-2026: الشاشة بتعرض عناوين تفصيلية وأسماء مناديب) |

> ⚠️ **`logistics_control_center` لازم تتسجّل في `ecommoda-constants` §7 قبل أول
> `writeLog`** — يعني قبل أول تسجيل دخول فعلي على الأداة. الصف المطلوب:
> `| Logistics Control Center | logistics_control_center | login · logout |`

---

## القرارات اللي اتحسمت وقت البناء (29-08-2026)

| القرار | الاختيار | ليه |
|---|---|---|
| **G-11** — `S1 = Returned` و `cancelledAt` فاضي | **`UNCLASSIFIED`** (قراءة Performance-Dashboard) | الخسارة تيجي من حدث شوبيفاي مش من ميتافيلد. الصف بياخد `S1_RETURNED_NO_CANCEL` + الإجراء (Cancel + Restock). **الأداتين دلوقتي بيقولوا نفس الرقم.** |
| الفلوس | **مستبعدة تمامًا** | سؤال مسئول الشحن تشغيلي مش مالي، والمرتجع الجزئي/الاستبدال محتاجين حساب على مستوى القطعة (`piece-level-valuation.md`) — خارج نطاق الأداة. |
| **تحميل الفترة** | **شهر بشهر** — الواجهة بتقسّم الفترة على حدود الشهور وبتنادي `get_data` مرة لكل شهر بالتسلسل مع شريط تقدّم | أحمد اختار "طلب واحد" الأول وغيّر رأيه (29-08). النتيجة: زرار التحديث بيسحب الشهر اللي لسه بيتغيّر بس (~٣ آلاف أوردر) بدل الست شهور (~٢٠ ألف)، وحجم كل مفتاح كاش بقى ~١.٥ ميجا بدل ٨–٩. |
| **كاش دائم** | 🔴 **ملغي تمامًا — TTL متدرّج بدل منه** | تفصيله تحت في "أهم قرار في الملف ده". |
| توقّع اكتمال الفترة (Rule 11) | **مش منفَّذ** | ما اتطلبش. لو اتطلب لاحقًا، `dashboard-builder` Rule 11 إلزامية بالكامل — معدلات مقاسة، حساب على مستوى الـ sub-bucket، ورجل المرتجع تتحسب خسارة. |
| فلاتر تاب "كل الأوردرات" | **بتأثّر على جدول التاب ده بس** | فلتر مخفي بيغيّر مربّع في تاب تاني = سكرين شوت لرقم غلط شكله سليم. |

## القرارات اللي اتحسمت بعد أول تسليم (30-08-2026)

| القرار | الاختيار | ليه |
|---|---|---|
| **تاريخ بداية البيانات** | 🔴 **`STORE_START_DATE = '2026-05-01'`** — مفيش بيانات قبله في أي مكان | قرار أحمد: بداية تشغيل المتجر فعليًا. تفصيله تحت في "🔴 قرار تاريخ بداية البيانات". |
| فلتر الفترة السريعة (تاب النظرة العامة) | **اتلغى بالكامل** — بدّله **٦ مربعات شهور** (الشهر الحالي + لحد ٥ سابقين، مقصوصة على `STORE_START_DATE`) — ⚠️ **رجعت لاحقًا كخيار ثانوي، راجع "القرارات اللي اتحسمت في التعديل السابع" تحت** | طلب صريح من صاحب الأداة. الضغط على مربّع = فلترة الشاشة كلها على الشهر ده. حقول "من/إلى تاريخ" لسه موجودة لفترة مخصّصة. |
| تحديث المربعات | **كل ضغطة على مربّع شهر بتسحب من شوبيفاي مباشرة، مش كاش-أولاً** | طلب صريح من صاحب الأداة — استثناء مقصود من قاعدة "التحديث بزرار أو تغيير فترة بس" في `dashboard-builder` (الضغطة على مربّع الشهر *هي* الزرار). التحميل الأول بعد الدخول لسه كاش-أولاً عادي. |
| جدول المقارنة الشهرية | 🔴 **اتلغى بالكامل** — مفيش بديل Δ حاليًا | طلب صريح من صاحب الأداة. القسم كان بيغطّي غياب Δ% على المربعات (راجع نسخة v1.0.0 من الملف ده) — الغياب ده رجع تاني من غير بديل. |
| دليل أسباب المراجعة | **Collapsible (`<details>`)، مقفول افتراضيًا** | طلب صريح من صاحب الأداة — القسم كان بياخد مساحة دايمة حتى لو مفيش صفوف تحتاج مراجعة كتير. |
| تاب "سجل الدخول" | 🔴 **اتلغى من الواجهة بالكامل** | طلب صريح من صاحب الأداة. `get_access_logs`/`_count`/`_export` **لسه موجودين في الـ Worker** (مفيش داعي نمسحهم — دخول/خروج لسه بيتسجّل في D1)، بس **مفيش تاب بيقراهم**. لو اتطلب رجوعهم لاحقًا، الـ endpoints جاهزة. |
| تاب "كل الأوردرات" | 🔴 **اتلغى — جدول الأوردرات اندمج في تاب "الصفحة الرئيسية"** (بعد قسم "دليل أسباب المراجعة"، آخر الصفحة) بنفس الفلاتر والترتيب وتصدير XLSX | طلب صريح من صاحب الأداة. فلاتر الجدول لسه بتأثّر على الجدول نفسه بس، مش على مربعات/رسوم "نظرة عامة" فوقه. |
| عمودا "سبب الإلغاء" و"سبب المرتجع" في جدول الأوردرات | 🔴 **اتشالوا من الجدول** | طلب صريح من صاحب الأداة. البحث النصي في الجدول لسه بيدوّر في السببين، بس مش معروضين كعمود منفصل. |
| تاب "التجهيز والطوابير" | 🔴 **اتقسم لتابين: 📦 تحت التجهيز و🚛 خرج للشحن** | طلب صريح من صاحب الأداة. "خرج للشحن" = مواعيد الاستلام. كل تاب فيه جدول تفصيلي مستقل (فلاتر اختيار متعدد + ترتيب + تصدير XLSX) زي جدول "الصفحة الرئيسية"، بمنطق فلترة مستقل تمامًا (`msStatePrep`/`msStateShipped`) عن جدول "الصفحة الرئيسية" (`msState`). |
| نطاق تاب "تحت التجهيز" (تعديل 30-08-2026) | 🔴 **بقى مطابق لمربّع `PREP` في `classifyOrder()`** (`PREP_CONFIRMED` + `PREP_EXCHANGE` + `PREP_RETURN`) — مش `S1 = Ready` بس زي الأول | قرار أحمد: التاب كان بيفلتر `state.orders` على `r.s1 === 'Ready'` مباشرة (تجاهل تام لـ `r.bucket`/S2)، فرقم كارت "أوردرات Ready" فيه (233) كان مختلف عن رقم كارت "تحت التجهيز" في الصفحة الرئيسية (258) — نفس التاريخ، نفس الفترة. الفرق: أوردرات لسه Confirmed/Confirmed+Edit (ما وصلتش Ready)، وأوردرات Delivered فيها دورة استبدال/مرتجع لسه في مرحلة التجهيز. `prepBaseRows()` بقت `bucketGroup(r.bucket) === 'PREP'`، وجدول التاب بقى فيه عمود "المرحلة" (`bucketLabel(r.bucket)`) يوضّح نوع كل صف. كارت "محاولة تسليم متكررة" بقى مقصور على `PREP_CONFIRMED` بس (عن طريق `isRepeatAttempt()`) — أوردرات `PREP_EXCHANGE`/`PREP_RETURN` أصلًا Delivered فـ FULFILLED عندها طبيعي، مش علامة تسليم متكرر. طابور الحالات المؤقتة (`TEMP_S1`) فوق الكروت متأثّرش — لسه بيفلتر بـ S1 مباشرة، مفهوم منفصل عن نطاق الجدول الرئيسي. |
| مربعات KPI في "تحت التجهيز"/"خرج للشحن" | **قابلة للضغط كفلتر سريع على الجدول تحتها** | طلب صريح من صاحب الأداة. الضغط على مربّع بيفلتر جدول التاب على أوردرات المربّع ده بس؛ ضغطة تانية على نفس المربّع بترجّع كل الصفوف. مربعات "نظرة عامة" **الكبيرة** فوق لسه مش قابلة للضغط — ⚠️ **حُدِّث 30-08-2026: المربعات الفرعية (bd-item) جوّه كروت "الصفحة الرئيسية" بقت قابلة للضغط** (راجع الجدول تحت). |
| سكرول الجداول | **سكرول رأسي مستقل داخل كل جدول** (`max-height` + `overflow-y:auto` على `.tbl-scroll`)، هيدر الأعمدة `position:sticky` | طلب صريح من صاحب الأداة، بالقياس على أداة تانية شافها. اتطبّق على كل جدول في الأداة (`.data-table` جوّه `.tbl-scroll`) مش بس التابين الجداد. |
| أسامي المناطق في العرض | **`Cairo+Giza` → "قاهرة وجيزة" و`Other_Regions` → "محافظات"** — تعريب واجهة بس، القيمة الخام في `custom.zone` متغيّرتش | طلب صريح من صاحب الأداة. مطبّق عن طريق `zoneLabel()` في كل مكان بيتعرض فيه اسم المنطقة (جداول، فلاتر، رسوم، تصدير XLSX) — قيم تانية زي `Show_Room` بتفضل زي ما هي (مش مطلوب تغييرها). |

## القرارات اللي اتحسمت في التعديل التالت (30-08-2026 — كروت وجداول التجهيز/الشحن)

| القرار | الاختيار | ليه |
|---|---|---|
| كروت "تحت التجهيز" | **بقى فيها نفس المربعات الفرعية (مؤكد/استبدال/مرتجع) اللي في كروت "الصفحة الرئيسية"** — تحت زرار "عرض التفاصيل" لكل كارت | طلب صريح من صاحب الأداة. المربعات الفرعية محسوبة على نطاق كل كارت (كل أوردرات التجهيز / لسه ما اتطبعش / اتطبع وما اتغلّفش / اتغلّف) مش على `prepBaseRows()` كله. |
| زرار "عرض التفاصيل" | **مفتاح واحد (`breakdownsOpen`) لكل الكروت في الأداة** — ضغطة على أي زرار بتفتح/تقفل كل الكروت اللي ليها تفاصيل فرعية مرة واحدة | طلب صريح من صاحب الأداة، قياسًا على Performance-Dashboard. قبل كده كان كل كارت له مفتاح فتح/قفل مستقل (`openBreakdowns` Set بمفتاح الكارت). |
| المربعات الفرعية (bd-item) | **قابلة للضغط زي المربعات الكبيرة — بتفلتر الجدول تحتها** | طلب صريح من صاحب الأداة، قياسًا على Performance-Dashboard. **النطاق:** تاب "الصفحة الرئيسية" (بتفلتر `msState.bucket`/`msState.finality`) وتاب "تحت التجهيز" (بتفلتر `msStatePrep.bucket`) — التابين دول بس عندهم مربعات فرعية أصلًا. تاب "خرج للشحن" مالوش مربعات فرعية (الأنواع التلاتة أصلًا كروت مستقلة قابلة للضغط)، وتاب "الإلغاء والمرتجعات" مربعاته بسيطة من غير تفاصيل فرعية أو جدول مفلتر بنفس المنطق — فمفيش عليهم تعديل. |
| كارت + عمود "محاولة تسليم متكررة" (تاب تحت التجهيز) | 🔴 **اتشالوا بالكامل** | طلب صريح من صاحب الأداة. `isRepeatAttempt()` والمنطق المرتبط بيه اتمسحوا من الكود بالكامل (مش مجرد إخفاء). |
| تصنيف "لسه ما اتطبعش" / "اتغلّف" (تاب تحت التجهيز) | 🔴 **تصحيح ازدواج عدّ** — أوردر عليه `packedAt` من غير `printedAt` كان بيتحسب في الكارتين مع بعض. دلوقتي "لسه ما اتطبعش" = مفيش طباعة **ولا** تغليف، و"اتغلّف" = عليه `packedAt` (بما فيهم الشذوذ ده) مع ملحوظة صريحة "تم التغليف بدون طباعة" على الكارت والصف في الجدول | قرار أحمد — الشذوذ التشغيلي (تغليف قبل الطباعة) لازم يبان في مكان واحد واضح، مش يختفي في عدّ مزدوج. |
| فلتر "دورة التجهيز" (تاب تحت التجهيز) | 🔴 **اتسمّى "مرحلة التجهيز"** | طلب صريح من صاحب الأداة. عمود "المرحلة" في نفس الجدول متغيّرش (اسم مختلف أصلًا). |
| ترتيب أعمدة جدولي "تحت التجهيز"/"خرج للشحن" | **كل الأعمدة بقت قابلة للترتيب** (المرحلة/الحالة · عمر الأوردر · اتطبع/اتغلّف · الملاحظات · وسم المراجعة) | طلب صريح من صاحب الأداة — كانت أعمدة نصية/محسوبة زي "عمر الأوردر" و"الملاحظات" بلا ترتيب. |
| جدول "مواعيد الاستلام" وكارت "بدون تاريخ استلام" (تاب خرج للشحن) | 🔴 **اتشالوا بالكامل** | طلب صريح من صاحب الأداة. عمود "تاريخ الاستلام" لسه موجود وقابل للترتيب في جدول الأوردرات نفسه — مفيش فقدان بيانات، بس القسم المجمّع المنفصل اتلغى. |

## تصحيح بعد أول استخدام للمربعات الفرعية (30-08-2026 — v1.5.1)

المربعات الفرعية اللي اتضافت في التعديل فوق كان فيها باج: كود الـ bucket نفسه
(`PREP_CONFIRMED`/`PREP_EXCHANGE`/`PREP_RETURN`) متكرر في الأربع كروت في تاب
"تحت التجهيز"، والفلتر القديم كان بيتفعّل على كود الـ bucket لوحده — فالضغط
على "استبدال" في أي كارت كان بيضيء "استبدال" في **الأربع كروت مع بعض**، وضغط
مربّع فرعي في كارت بعد ما كارت كبير تاني كان متفلتر كان بيرجّع صفوف فاضية
(الفلتر القديم `prepQuick` فاضل عالق على الكارت الأول).

**الحل:** `prepBdClick(cardKey, bucket)` بقى بياخد هوية الكارت مع كود الـ
bucket مع بعض، وبيحدّث `prepQuick` **و** `msStatePrep.bucket` سوا — فكل مربّع
فرعي بيضيء مربّعه بس (مش نظيره في كارت تاني)، وبيفلتر الجدول على تقاطع الكارت
+ النوع الصح. الضغط على أي كارت كبير (`prepQuickToggle`) بقى كمان بيمسح أي
فلتر bucket متسيّب من مربّع فرعي في كارت تاني. اتفحص فعليًا في Chromium
headless بأوردرات تركيبية تغطي التقاطعات الأربعة (notPrinted/printedNotPacked/
packed × كل نوع) — كل تقاطع بيرجّع صفوفه الصح بس.

## القرارات اللي اتحسمت في التعديل الرابع (30-08-2026 — تقسيم تاب المناديب والمناطق)

| القرار | الاختيار | ليه |
|---|---|---|
| تاب "المناديب والمناطق" | 🔴 **اتقسم لتابين مستقلين: 🚚 المناديب و🗺️ المناطق** | طلب صريح من صاحب الأداة. رسم "أوردرات كل مندوب" وجدولا "المناديب" الجداد راحوا لتاب `panelDelegates` (`renderDelegatesTab`)؛ رسم "توزيع المناطق" وجدولا "أداء المناطق"/"أعلى 25 مدينة" فضلوا زي ما هم في `panelRegions` (`renderRegionsTab`) — بلا تعديل على منطقهم. |
| ترتيب التابات | **نظرة عامة · تحت التجهيز · خرج للشحن · المناديب · المناطق · الإلغاء والمرتجعات** | طلب صريح من صاحب الأداة. `TAB_IDS` اتحدّث بنفس الترتيب — ترتيب التابات في الواجهة والقائمة المنسدلة (`buildTabChipMenu`) بيتبع ترتيب المصفوفة دي، مش ترتيب الـ `<div class="tab-panel">` في الـ DOM (كل التابات `display:none` إلا النشط، فترتيب الـ DOM مالوش تأثير بصري). |
| جدول "أداء المناديب" (تاب المناديب) | 🔴 **اتشال بالكامل** — بدّله جدولين لكل مندوب من **زاويتين** | طلب صريح من صاحب الأداة: "احصائيات بكل مندوب من زاويتين — لو الأوردر لسه انتقالي ولو حالة نهائية، والتركيز أكتر على الانتقالي عشان مسئول الشحن يعرف الأوردرات اللي لسه متقفلتش وتفاصيلها ايه". |
| **زاوية ١ — جدول "أوردرات لسه متقفلتش"** (`delegateTransitTable`) | عمود لكل bucket انتقالي (`PENDING_CONFIRM` · `PREP_CONFIRMED/EXCHANGE/RETURN` · `SHIPPED_CONFIRMED/EXCHANGE/RETURN` · `UNCLASSIFIED`) + الإجمالي الانتقالي + **أقدم أوردر لسه مفتوح** (لينك + عمره، ملوّن أصفر/أحمر زي طابور الحالات المؤقتة) + **متوسط العمر** | التركيز على الانتقالي (طلب أحمد) بيتحقق بعمودي العمر — دول مش موجودين في جدول الزاوية التانية. `aggregateDelegateStage(rows, DELEGATE_TRANSIT_BUCKETS, true)` — بارامتر `withAge=true`. |
| **زاوية ٢ — جدول "الأوضاع النهائية"** (`delegateFinalTable`) | عمود لكل bucket نهائي (`DELIVERY_BASIC/EXCHANGE/PARTIAL_RETURN` · `LOST_CANCELLED/RTO/FULL_RETURN`) + الإجمالي النهائي + نسبة التسليم/الرفض (نفس معادلة `aggregateBy` القديمة) | بديل مباشر للجدول القديم المحذوف، بس بتفصيل أدق (٣ أنواع تسليم منفصلة بدل رقم "تسليمات" واحد). `aggregateDelegateStage(rows, DELEGATE_FINAL_BUCKETS, false)`. |
| كل delegate بيظهر في الجدولين حتى لو صفر | **مقصود** | صف بصفر في الزاوية الانتقالية معناه "كل أوردرات المندوب ده مقفولة" — إشارة مفيدة لمسئول الشحن، مش سطر ناقص. |
| اسم مربّع `PREP_CONFIRMED` | 🔴 **"مؤكد / تحت التجهيز" → "أساسي / تحت التجهيز"** | طلب صريح من صاحب الأداة، في كل مكان (`BUCKET_META`، فبيتوّرث تلقائيًا لكل الكروت والجداول والفلاتر اللي بتستخدم `bucketLabel()`). |
| اسم مربّع `SHIPPED_CONFIRMED` | 🔴 **"مؤكد / خرج للشحن" → "أساسي / خرج للشحن"** | نفس السبب فوق. |
| اسم مربّع/كارت `LOST` | 🔴 **"خسائر" → "إلغاءات + مرتجعات"** | طلب صريح من صاحب الأداة، في كل مكان (`GROUP_META.LOST.label` + legend رسم تاب "المناديب" اللي كان بيكتب "خسائر" ثابتة بدل ما يقرا من `GROUP_META`). |

## القرارات اللي اتحسمت في التعديل الخامس (30-08-2026 — كروت المناديب + تبسيط الجداول + دمج جداول المناطق)

| القرار | الاختيار | ليه |
|---|---|---|
| رسم "أوردرات كل مندوب (أعلى ١٠)" (تاب المناديب) | 🔴 **اتشال بالكامل** — بدّله كارت ملخص لكل مندوب | طلب صريح من صاحب الأداة. الرسم كان مقصور على أعلى ١٠ مناديب بس؛ الكارت الجديد بيغطي كل المناديب. |
| كارت ملخص المندوب (`renderDelegateCards` بيستخدم `aggregateBy(rows, r => r.courier)`) | إجمالي · تسليم · إلغاء/مرتجع (`rto+fullReturn+cancelled`) · نسبة تسليم بشريط تقدّم ملوّن — **بلا شريط لنسبة المرتجعات** | طلب صريح من صاحب الأداة، بالقياس على سكرين شوت من أداة تانية. الكروت **قابلة للضغط** (`delegateCardClick`) — بتفلتر جدولي التاب تحتها (`delegateQuick`) على المندوب المختار، زي باقي كروت الأداة (`prepQuickToggle`/`overviewBdClick`). ده كمان الرد على "فين الفلاتر؟" — التابين مكانش فيهم أي آلية فلترة/تفاعل قبل كده، عكس باقي التابات. |
| أعمدة جدولي "المناديب" (الانتقالي والنهائي) | 🔴 **الأعمدة الفرعية التفصيلية (PREP_CONFIRMED/EXCHANGE/RETURN، DELIVERY_BASIC/EXCHANGE/PARTIAL_RETURN، إلخ) اتستبدلت بأعمدة المربعات الأساسية** (تحت التأكيد/تحت التجهيز/خرج للشحن/خارج التصنيف/تسليمات/إلغاءات + مرتجعات — نفس `GROUP_META`) | طلب صريح من صاحب الأداة. التفصيل الفرعي لسه محسوب داخليًا (`withGroupTotals` بيجمّع الأعمدة الفرعية لأعمدة أساسية) لأغراض نسب التسليم/الرفض، بس مش معروض كعمود منفصل — التفصيل الفردي (مؤكد/استبدال/مرتجع لكل حالة) بقى غير متاح من الجدولين، الكارت مبيوضحوش هو تفصيل تاني (إجمالي/تسليم/إلغاء-مرتجع). |
| عمود "متوسط العمر" (جدول "أوردرات في مرحلة انتقالية") | 🔴 **اتشال بالكامل** | طلب صريح من صاحب الأداة. عمود "أقدم أوردر لسه مفتوح" فضل زي ما هو. |
| حجم خط الأرقام في جدولي المناديب | **كبّر شوية** (`.dt-lg-num` — 15px بدل 12.5px الافتراضي) | طلب صريح من صاحب الأداة. |
| عنوان "أوردرات لسه متقفلتش" | 🔴 **"أوردرات في مرحلة انتقالية"** | طلب صريح من صاحب الأداة (إعادة تسمية بس — نفس الجدول ونفس المنطق). |
| عنوان "الأوضاع النهائية" | 🔴 **"أوردرات ذات حالة نهائية"** | نفس السبب فوق. |
| جدولا "أداء المناطق"/"أعلى ٢٥ مدينة" (تاب المناطق) | 🔴 **نفس تجربة جدولي "المناديب" (أعمدة المربعات الأساسية + أقدم أوردر لسه مفتوح + نسب التسليم/الرفض) بس مدموجين في جدول واحد** بدل جدولين منفصلين (انتقالي/نهائي) — `aggregateEntityMerged(rows, keyFn)` | طلب صريح من صاحب الأداة: "نقل نفس تجربة جداول حالة كل مندوب... يتم دمج الجدولين". دالة `aggregateEntityMerged` عامة بـ `keyFn` — مستخدمة لكل من `r => r.zone` و`r => r.city`؛ الإجمالي = مجموع الإجمالي الانتقالي + النهائي (يغطي كل الـ ١٤ bucket، زي `aggregateBy` القديمة بالظبط). |
| رسوم بيانية إضافية للمدينة (تاب المناطق) | 🆕 **رسمين جداد**: "توزيع الأوردرات لأعلى ١٥ مدينة" (عمود مكدّس تسليمات/في الطريق/إلغاءات+مرتجعات — نفس تصميم الرسم المحذوف من تاب المناديب) و"نسبة التسليم لأعلى ١٥ مدينة" (عمود أفقي ملوّن بالعتبات المعتادة ٨٠٪/٦٠٪) | طلب صريح من صاحب الأداة — مكانش فيه أي رسم للمدينة قبل كده، غير جدول "أعلى ٢٥ مدينة". رسم "توزيع المناطق" (Donut) اتسيب زي ما هو. |
| `aggregateDelegateStage(rows, bucketList, withAge, keyFn)` | بقت بتاخد `keyFn` اختياري (افتراضيًا `r => r.courier`) بدل `r.courier` مكتوبة جوّاها | ضروري عشان تتستخدم من `aggregateEntityMerged` بمفتاح زون/مدينة كمان — من غيره التجميع كان هيفضل بيتجمّع بالمندوب حتى لو الجدول بيعرض مناطق/مدن. |

**اختبار:** `node --check` على الـ JS المستخرج من الملف — سليم. فحص فعلي في Chromium headless
ببيانات تركيبية (مناديب/مناطق/مدن متعددة عبر الـ ١٤ bucket): كروت المناديب اتعرضت
صح، الضغط على كارت فلتر الجدولين على مندوب واحد بس وضغطة تانية رجّعت كل الصفوف،
عدد أعمدة كل جدول طابق عدد الخلايا في كل صف (٧/٦ لجدولي المناديب، ١١ لجدولي
المناطق/المدن المدموجين)، والعنوانين الجداد ("أوردرات في مرحلة انتقالية"/"أوردرات
ذات حالة نهائية") ظهروا صح. الرسوم البيانية اتفحصت بالتدهور الآمن بس (Chart.js
ما حمّلش في بيئة الفحص المعزولة) — لسه محتاجة تأكيد بصري على نشر حقيقي.

## القرارات اللي اتحسمت في التعديل السادس (30-08-2026 — كروت انتقالية + جدول تفصيلي + فلاتر المناديب + دونات المناطق)

| القرار | الاختيار | ليه |
|---|---|---|
| كارت ملخص المندوب | 🔴 **إحصائياته بقت انتقالية** (إجمالي · تحت التجهيز · تحت الشحن) بدل النهائية (تسليم/إلغاء-مرتجع) — الشريط الملوّن لنسبة التسليم اتشال معاها | طلب صريح من صاحب الأداة: "أريدها تكون للحالة الانتقالية". `aggregateBy()` بقت بتحسب `prep`/`shipped` (عدد `bucketGroup === 'PREP'`/`'SHIPPED'`) جنب الحقول القديمة (لسه محسوبة، مستخدمة في رسوم تاب المناطق). |
| المندوب الفاضي | 🔴 **"(بدون مندوب)"** بدل "(غير محدد)" | طلب صريح، وبيطابق التسمية المستخدمة أصلًا في فلاتر `courier` بباقي الأداة (`msState.courier`/`msStatePrep.courier`/`msStateShipped.courier`). `aggregateBy`/`aggregateDelegateStage` بقى ليهم بارامتر `noneLabel` اختياري (افتراضيًا "(غير محدد)" لسياقات المنطقة/المدينة) بدل ما يكون الاسم مكتوب جوّه الدالة. |
| ضغط كارت المندوب | 🆕 **بيفتح تحت الكروت جدول تفصيلي بكل أوردرات المندوب ده** (`renderDelegateOrdersTable`) — نفس أعمدة جدول "الصفحة الرئيسية" **ماعدا عمود "المرحلة"** (بيقول "أساسي/استبدال/مرتجع" + "تحت التجهيز/خرج للشحن" مع بعض — مش لازم هنا لأنه بيغطي كل أوضاع المندوب مش مرحلة واحدة). بترقيم صفحات (١٠٠ صف) لمناديب زي Bosta اللي عندهم آلاف الأوردرات. **مش متأثّر بفلاتر الجدولين تحت** — دايمًا كل أوردرات المندوب المختار، بصرف النظر عن أي فلتر تاني. |
| فلاتر جدولي "أوردرات في مرحلة انتقالية"/"أوردرات ذات حالة نهائية" | 🆕 **فلاتر اختيار متعدد كاملة** (المرحلة · المنطقة · المدينة · الشهر · وسم المراجعة · عليه ملاحظة) + بحث نصي — نسخة مستقلة (`msStateDelegates`) زي `msStatePrep`/`msStateShipped` بالظبط | طلب صريح: "فين الفلاتر مثل باقي الجداول؟". **بدون فلتر "المندوب" عمدًا** — هو أصلاً بُعد التجميع (كل صف = مندوب)؛ اختيار مندوب واحد بيتم بالضغط على كارته (`delegateQuick`) مش من هنا. الفلاتر بتضيّق `state.orders` **قبل** التجميع بالمندوب، فبتأثّر على الجدولين مع بعض دفعة واحدة (مش على كارت الملخص ولا الجدول التفصيلي — نفس قاعدة "فلاتر الجدول بتأثّر على الجدول ده بس"). |
| دونات "توزيع المناطق" (تاب المناطق) | 🔴 **بقى معاها بيانات جانبية** (نقطة ملوّنة + اسم المنطقة + العدد + النسبة لكل شريحة) بدل الاعتماد على ليجند Chart.js تحت الرسم بس — الليجند القديم اتقفل (`legend:{display:false}`) | طلب صريح، بالقياس على تصميم "توزيع الأوردرات" في `Performance-Dashboard` (`.chart-legend`/`.chart-legend-item`). كلاسات CSS جديدة `.chart-box-split`/`.chart-box-canvas`/`.chart-side-legend*` — الكانفس والليجند جنب بعض في نفس الصف (`display:flex`) بدل الكانفس لوحده في `.chart-box`. ⚠️ **فخ اتصلح وقت البناء:** `margin:0 auto` على صندوق الكانفس (متبقّي من نمط `.chart-box` القديم لكارت واحد بيتمركز لوحده) بيمتص كل المساحة الفاضية في الصف كـ margin تلقائي على الكانفس نفسه (سلوك CSS موثّق للـ flex auto-margins) — بيدفع الليجند لطرف الحاوية بدل ما يفضل جنب الكانفس. اتشال الـ margin، والتمركز بقى مسؤولية `.chart-box-split` (`justify-content:center`) بس. |

**اختبار:** `node --check` على الـ JS المستخرج من الملف — سليم. فحص فعلي في Chromium
headless ببيانات تركيبية (٣٠٠ أوردر، ٣ مناديب بينهم "(بدون مندوب)"، عبر الـ ١٤
bucket): الكروت بتعرض إجمالي/تحت التجهيز/تحت الشحن صح · ضغط كارت بيفتح الجدول
التفصيلي (١٤ عمود، بدون عمود المرحلة، بترقيم صفحات) وضغطة تانية بتقفله · اختيار
فلتر بيضيّق الجدولين والعداد وزرار "مسح الفلاتر" بيرجّع الكل · دونات المناطق
بتعرض بيانات جانبية صح (نسبة + عدد لكل منطقة) بعد إصلاح فخ الـ `margin:auto`.
Chart.js نفسها ما اتفحصتش بصريًا (محجوبة في بيئة الفحص) — لسه محتاجة تأكيد على
نشر حقيقي.

## القرارات اللي اتحسمت في التعديل السابع (30-08-2026 — فترة سريعة ثانوية + ترتيب مربعات الشهور)

| القرار | الاختيار | ليه |
|---|---|---|
| فلتر "فترة سريعة" (تاب النظرة العامة) | 🔴 **رجع كخيار ثانوي/اختياري جنب مربعات الشهور** — تعديل جزئي لقرار 30-08-2026 فوق ("اتلغى بالكامل") | طلب صريح من صاحب الأداة: عايز نفس كومبوننت الفترة السريعة الموجود في `Performance-Dashboard` (اليوم/أمس/آخر 3-90 يوم/📅 شهر محدد/الشهر أو الربع أو العام الحالي والسابق)، **بس مربعات الشهور تفضل الفلتر الأساسي** — مش استبدال تاني. الفرق عن القرار القديم: مفيش إلغاء، الاتنين موجودين مع بعض، وواحد بس نشط في كل لحظة. |
| سلوك التحديث لفتح فترة سريعة | **كاش-أولاً** (زي تعديل يدوي للتاريخ) — **مش** زي ضغطة مربّع شهر (اللي بتفرض سحب مباشر من شوبيفاي) | مربّع الشهر عنده استثناء مقصود ("الضغطة *هي* الزرار" — راجع جدول 30-08-2026 الأول)؛ الفترة السريعة مش استثناء جديد، هي طريقة تانية لتحديد `from`/`to` فبتتبع نفس قاعدة "التحديث بزرار أو تغيير فترة بس" في `dashboard-builder`. |
| تعارض الحالة بين الفلترين | **مربّع الشهر وفلتر الفترة السريعة بيمسحوا بعض** — اختيار أي واحد بيصفّر التاني (`activeMonthYm` أو `activeRangePreset`) عن طريق `resetRangePresetUI()` | من غيره القائمتين ممكن يفضلوا عارضين حالتين متناقضتين مع بعض (مربّع مضيء + preset تاني مختار)، وده بالظبط نوع اللبس اللي "رقم غلط شكله سليم" بييجي منه. |
| فترة سريعة كلها قبل `STORE_START_DATE` (زي "العام السابق" حاليًا) | **بترفض بتوست تحذير** بدل ما تتقصّ بصمت لـ `from > to` | التقصّ الصامت الموجود أصلاً لتعديل التاريخ اليدوي (`clampFromFloor` على `from` بس) كان هيسيب `to` قبل `from` لو الفترة كلها قبل بداية التشغيل — طلب غير صالح مبيتبعتش لشوبيفاي، والمستخدم لازم يعرف ليه بدل ما الشاشة تفضل فاضية من غير سبب. |
| ترتيب مربعات الشهور | 🔴 **اتقلب — الشهر الحالي بقى أول مربّع (أقصى اليمين، في RTL)**، والأقدم آخر واحد (أقصى الشمال) | طلب صريح من صاحب الأداة. `computeMonthBoxes()` كانت بترجّع الأقدم أولاً (`boxes.reverse()`) فكان الشهر الحالي بيبان أقصى الشمال — اتشالت `.reverse()` فبقت المصفوفة الأحدث أولاً، ومطابقة إن أول عنصر في الـ DOM بيظهر أقصى اليمين في RTL. `initDefaultMonth()` اتحدّثت تاخد `boxes[0]` (الأحدث) بدل `boxes[boxes.length-1]`. |

**اختبار:** `node --check` على الـ JS المستخرج من الملف — سليم. فحص فعلي في Chromium
headless (بدون Worker منشور، حالة دخول مموّهة في الذاكرة): ترتيب مربعات الشهور
اتأكد بصريًا (أغسطس ٢٠٢٦ أول مربّع/أقصى اليمين → يوليو → يونيو → مايو آخر واحد) ·
فتح قائمة "فترة سريعة" واختيار "آخر ٧ أيام" ملا حقلي التاريخ صح ومسح تفعيل مربّع
الشهر · الضغط على مربّع شهر بعدها رجّع تسمية الفترة السريعة لحالتها المحايدة
("⚡ اختار فترة") · اختيار "العام السابق" (٢٠٢٥، كله قبل `STORE_START_DATE`) اتفحص
وطلّع توست التحذير من غير ما يغيّر حقلي التاريخ · اختيار "العام الحالي" (فترة
جزئية قبل `STORE_START_DATE`) قصّ `from` على `2026-05-01` صح · "📅 شهر محدد"
(يونيو ٢٠٢٦) طبّق الفترة واسم الليبل صح. صفر أخطاء console (الأخطاء الوحيدة
كانت شبكة محجوبة — CDN/خطوط، متوقّعة في بيئة الفحص المعزولة).

## القرارات اللي اتحسمت في التعديل التامن (30-08-2026 — شبكة الكاش والبوابة وحارس المربّع)

التعديل ده جه من **مراجعة مقارنة** بين الأداة دي و`Performance-Dashboard` —
سؤال "إيه الفرق بين طريقتي تحميل البيانات؟". المراجعة كشفت إن اتنين من التلات
مشاكل **قواعد كانت مكتوبة في `ecommoda-dashboard-builder` نفسها**، فاتصلّحت في
المهارة الأول (v2.1.0) وبعدين في الأداة.

| القرار | الاختيار | ليه |
|---|---|---|
| **حدود الـ chunk** | 🔴 **شهور تقويمية كاملة — القصّ على حدود الفترة المختارة اتشال** | مفتاح الكاش هو **نصّ التاريخين حرفيًا**، فالقصّ كان بيولّد مفتاح يتيم لكل فترة مخصصة: `data:2026-08-01:2026-08-30` ("آخر ٣٠ يوم") مالوش أي علاقة بـ`data:2026-08-01:2026-08-31` (مربّع أغسطس) رغم **فرق يوم واحد**، و"آخر ٧ أيام" كانت بتولّد مفتاح جديد **كل يوم**. النتيجة: صفر إعادة استخدام لكاش الشهر الموجود أصلًا، وسحب مكرر، وبوابة بتظهر على فترة كل بياناتها متكاشة. الأطراف بقت بتتفلتر في الواجهة (`visibleRows` بـ `createdAt`) — **الفترة المعروضة والليبلات ما اتغيّروش**. |
| قصّ أول شهر على `STORE_START_DATE` | **باقي — وده مش نفس القصّ اللي اتشال** | حدّ **ثابت** مش بيعتمد على اختيار المستخدم، فالمفتاح بيفضل ثابت. وهو كمان بيطابق `clampFrom` في الـ Worker، فمفتاح الواجهة ومفتاح الـ Worker ما يفترقوش. |
| **البوابة** ("فيه شهور لسه ما اتحمّلتش") | 🔴 **بالتكلفة مش بوجود نقص** — شهر واحد ناقص بيتحمّل تلقائيًا بشريط تقدّم، البوابة من شهرين فما فوق (`GATE_MIN_MISSING_MONTHS = 1`) | مفتاح الـ `meta` بياخد **نفس TTL** الـ `data`، والشهر المفتوح TTL بتاعه ١٥ دقيقة — فالشهر الحالي بيبان "ناقص" بعد أي ربع ساعة سكون، **وده الوضع الطبيعي مش استثناء**. البوابة كانت بتظهر في كل فتح للأداة، وده **خرق مباشر** لـ`ecommoda-html-builder` `date-range-filter.md` §1 («لا يوجد زرار "تطبيق" منفصل») و§5 («لازم preset افتراضي يكون متطبّق ومعروض»): هي زرار التطبيق راجع من الشباك. |
| علاج البوابة بإطالة TTL الـ `meta` | ⛔ **مرفوض صراحةً** | هيخلي الـ probe يقول "متكاش" وهو مش متكاش — استبدال إزعاج بكذب. الـ `meta` والـ `data` بيموتوا مع بعض عمدًا. |
| **حارس حداثة على مربّع الشهر** | 🆕 `MONTH_BOX_FRESH_MINS = 5` — أقل من ٥ دقايق يقرا من الكاش، أكتر يسحب مباشر — ⚠️ **القيمة بقت ١٥ في 01-09-2026، راجع القسم تحت** | الاستثناء المقصود ("الضغطة *هي* الزرار") كان **بلا أي حارس**: أغسطس ← يوليو ← أغسطس تاني = تلات دورات GraphQL كاملة في دقيقة، الأخيرة بلا أي فايدة. الاستثناء نفسه باقي لأي شهر مش طازة. |
| `clear_cache` الكامل | 🔴 **بقى بيلفّ على الـ `cursor`** | نداء `list()` واحد سقفه ١٠٠٠ مفتاح وكان بيرجّع `ok:true` وهو سايب مفاتيح ورا — مسح ناجح شكله سليم. `Performance-Dashboard` صلّحها 30-08؛ الأداة دي **أكتر عرضة** لأن التقسيم الشهري بيراكم مفاتيح أسرع. |
| بانر "أوردرات مقصوصة" | **بيتقصّ على المعروض** | أوردر مقصوص برّه الفترة المختارة مالوش صف على الشاشة — تحذير باسمه بيبعت المستخدم يدوّر على حاجة مش قدامه. |
| `CACHE_VERSION` | **فضل `v1` — مفيش bump** | شكل الصف وقائمة حقول `ORDER_QUERY` ما اتغيّروش. اللي اتغيّر هو **أنهي مفاتيح بتتطلب**، ومفاتيح الشهور الكاملة الموجودة أصلًا (من ضغطات مربعات الشهور) صالحة زي ما هي. رفع النسخة كان هيرمي كاش سليم ويفرض سحب كامل — تكلفة بلا أي فايدة. المفاتيح المقصوصة القديمة بقت يتيمة وبتموت بالـ TTL. |

## القرارات اللي اتحسمت في التعديل التاسع (31-08-2026 — كروت "الصفحة الرئيسية" قابلة للضغط + حذف رسمي التوزيع)

| القرار | الاختيار | ليه |
|---|---|---|
| الكروت الكبيرة في تاب "الصفحة الرئيسية" | 🆕 **بقت قابلة للضغط** (`overviewCardClick(buckets)`) — الضغط بيحط **كل** الـ buckets بتاعة الكارت في `msState.bucket` مع بعض (خرج للشحن = `SHIPPED_CONFIRMED` + `SHIPPED_EXCHANGE` + `SHIPPED_RETURN`)، وضغطة تانية بتمسحهم | طلب صريح من صاحب الأداة. المربعات الفرعية بس هي اللي كانت قابلة للضغط (قيمة واحدة) — الكارت نفسه مكانش ليه أي تفاعل رغم إنه شكله زي كروت "تحت التجهيز"/"خرج للشحن" القابلة للضغط. كارت "إجمالي الأوردرات" **مش** قابل للضغط: مربعاته الفرعية `finality` مش buckets، و"نشط" عنده معناها "مفيش فلتر" — إضاءة دايمة من أول تحميل. |
| الضغط على كارت كبير و فلتر "نوع الحالة" (`finality`) | **الكارت بيمسح `msState.finality`** | من غير كده اختيار متبقّي من كارت "إجمالي الأوردرات" (وضع نهائي) + كارت "خرج للشحن" = تقاطع فاضي: جدول بصفر صفوف وكارت مضيء بـ٢٧٨. نفس منطق `prepQuickToggle` اللي بيمسح `msStatePrep.bucket`. |
| إضاءة الكارت (`isCardFilterActive`) | **تطابق تام مع مجموعة الـ buckets** — لا أكتر ولا أقل | مربّع فرعي واحد متفلتر (`size === 1`) مالوش يضوّي الكارت كله، والعكس. كارت `PENDING` (bucket واحد) الاتنين بيضوّوا مع بعض — وده صح، هو حرفيًا نفس الفلتر. |
| مزامنة الإضاءة مع الفلاتر | 🔴 **`onMsFilterChanged` و`clearAllFilters` بقوا بينادوا `renderOverviewKpis()`** | الإضاءة بتتقرا من `msState`، فتغيير الفلتر من القايمة المنسدلة أو الشيب أو "مسح كل الفلاتر" كان بيسيب الكارت/المربّع مضيء وفلتره اتشال. **الأرقام نفسها ما بتتأثرش بالفلاتر** — `renderOverviewKpis` بتحسب من `state.orders` كله زي ما هي؛ اللي بيتحدّث هو الإضاءة بس. |
| شيب فلتر لـ bucket عدده صفر | **`bucketLabel(val)` كـ fallback في `renderChips`/`msUpdateButtonLabel`** | `msState.bucket.items` بتتبني من البيانات المحمّلة بس، وضغطة الكارت بتختار كل الـ buckets بتاعته حتى اللي مالوش صفوف في الفترة دي — فالشيب كان هيعرض الكود الخام (`SHIPPED_RETURN`) بدل الاسم العربي. |
| رسما "توزيع الأوردرات على المراحل" (دونات) و"نفس التوزيع — مقارنة بالأعمدة" | 🔴 **اتشالوا بالكامل** — `renderOverviewCharts()` و`barValueLabelsPlugin()` اتمسحوا من الكود (مش إخفاء) | طلب صريح من صاحب الأداة. الرسمين كانوا بيرسموا نفس أرقام الكروت اللي فوقهم بالظبط. `donutValueLabelsPlugin()` **باقية** — دونات "توزيع المناطق" في تاب المناطق لسه بيستخدمها. بانر "تعذّر تحميل مكتبة الرسوم" باقي كمان — تاب المناطق لسه فيه ٣ رسوم. |

**اختبار:** `node --check` على `index.js` وعلى الـ JS المستخرج من `index.html` — الاتنين سليمين.
فحص فعلي في Chromium headless بـ١١١ أوردر تركيبي موزّعين على الـ١٤ bucket (وفيهم
`SHIPPED_RETURN` بصفر عمدًا)، ٨ سيناريوهات كلها نجحت: ① ضغط كارت "خرج للشحن" فلتر
الجدول على ١٣ صف = مجموع مربعاته التلاتة، والشيبات طلعت بأسماء عربية · ② ضغطة تانية
رجّعت الـ١١١ · ③ مربّع فرعي واحد فلتر على ٧ صفوف **من غير** ما يضوّي الكارت ·
④ `finality=final` متسيّب قبل ضغطة كارت "تحت التجهيز" اتمسح والجدول طلع ١٢ (مش صفر) ·
⑤ "مسح كل الفلاتر" طفّى إضاءة كل الكروت · ⑥ الكارت اللي فيه bucket بصفر مطلّعش كود
خام في الشيبات · ⑦ `#overviewCharts`/`#boxDonut`/`#boxBar`/`renderOverviewCharts`
كلهم مش موجودين، ودونات المناطق (`#boxZone`) لسه موجود · ⑧ التنقّل بين التابات
شغّال. صفر أخطاء console/pageerror (غير الشبكة المحجوبة — CDN/خطوط).

## القرارات اللي اتحسمت في التعديل العاشر (31-08-2026 — المحافظة بدل المدينة + إعادة تسمية تابين)

| القرار | الاختيار | ليه |
|---|---|---|
| **البُعد الجغرافي في الأداة كلها** | 🔴 **`shippingAddress.province` بدل `shippingAddress.city`** — كل عمود وفلتر ورسم وجدول وتصدير وبحث نصي اتحوّل. المفتاح الداخلي اتسمّى `province` كمان (مش `city` بقيمة تانية) عشان `applySort` بتقرا `a[sortState.key]` من الصف مباشرة — مفتاح باسم قديم على حقل جديد كان هيكسر الترتيب بصمت. | طلب صريح من صاحب الأداة. `city` خانة **نص حر** بيكتبها اللي بيعمل الأوردر، فكانت بتيجي فيها اسم محافظة (`Red Sea`) أو عنوان كامل أو فاضي — بُعد تجميع بمئات القيم اللي معظمها نفس المكان مكتوب بشكل مختلف. `province` خانة **Governorate**: قائمة منسدلة مقفولة عند ٢٧ قيمة. |
| `city` في `ORDER_QUERY` | 🔴 **اتشال من الـ Worker خالص** (`v1.1.2`) — مش مجرد إخفاء في الواجهة | مفيش حاجة بتقراه. سيبانه كان هيكرر بالظبط المشكلة اللي التعديل ده بيصلحها من الناحية التانية: حقل متسحوب ومتكاش ومحدش بيقراه (وده كان وضع `province` نفسه قبل النهاردة). العنوان التفصيلي لسه كامل في `address1`، فمفيش فقدان معلومة تشغيلية. |
| `CACHE_VERSION` | **فضل `v1` — مفيش bump** | `province` موجود في `ORDER_QUERY` **من أول commit**، يعني كل صف متكاش في KV دلوقتي شايله. القاعدة (`أي حقل جديد → bump`) بتحمي من **حقل ناقص في صف قديم** — والحقل هنا مش جديد. اللي اتشال (`city`) مالوش قارئ، فصف قديم شايله = بايت زيادة، مش رقم غلط. رفع النسخة كان هيرمي كاش سليم ويفرض سحب كامل بلا فايدة. |
| سقف "أعلى ٢٥" على جدول البُعد الجغرافي | 🔴 **اتشال — الجدول بقى "أداء المحافظات" بكل الصفوف** | السقف كان منطقي على بُعد بمئات القيم الحرة. على مجموعة مقفولة من ٢٧، هو بيخفي صفين **من غير أي مؤشر على الشاشة** — "رقم غلط شكله سليم" حرفيًا. سقف الـ ١٥ على **الرسمين** باقي، لأنه سقف **قراءة** (٢٧ عمود مكدّس غير مقروء) والرقم مكتوب في عنوان الرسم نفسه. |
| تعريب أسامي المحافظات | **مفيش — بتتعرض خام بالإنجليزي زي شوبيفاي** | ما اتطلبش. لو اتطلب لاحقًا، المكان الصح دالة `provinceLabel()` على نمط `zoneLabel()` الموجودة — تعريب عرض بس، والقيمة الخام ما تتغيّرش. |
| اسم تاب "الصفحة الرئيسية" | 🔴 **"الصفحة الرئيسية"** | طلب صريح. التسمية اتغيّرت في زرار التاب بس — `TAB_IDS`/`panelOverview`/`renderOverviewKpis` وكل المعرّفات الداخلية زي ما هي (`updateTabChip`/`buildTabChipMenu` بيقروا نص الزرار، فالشيب والقائمة المنسدلة اتحدّثوا لوحدهم). |
| اسم تاب "المناطق" | 🔴 **"المحافظات"** (🗺️ → 🏛️) | طلب صريح. ⚠️ **"المنطقة" (`custom.zone`) لسه بُعد مستقل جوّه التاب** بدونات "توزيع المناطق" وجدول "أداء المناطق". وتصادف إن `zoneLabel('Other_Regions')` = **"محافظات"** — نفس كلمة اسم التاب وبُعد مختلف تمامًا. اتضاف بند صريح في "حدود معروفة" في مودال About عشان اللبس ده يتقال بدل ما يتخمّن. |

---

## 🔴 تعديل 01-09-2026 — حارس مربّع الشهر بقى = `TTL_OPEN` (`index.html` v1.12.2)

`MONTH_BOX_FRESH_MINS` كانت **٥ دقايق** بينما `TTL_OPEN` في `index.js` **١٥
دقيقة**. رقمين مستقلين على نفس السؤال ("البيانات دي طازة؟") — فالنافذة بينهم
(٥ → ١٥ دقيقة) كانت **سحب مضمون بلا أي فايدة**: الكاش لسه حي وصالح، والضغطة
بتفرض `forceRefresh:true` فيرجع الـ Worker **نفس** الصفوف بعد دورة GraphQL
كاملة. مرصودة فعليًا: مؤشر الطزاجة بيقول "من ٦ دقيقة" والشاشة بتدخل
"بنسحب من شوبيفاي".

**القرار (أحمد 01-09-2026):** القيمة بقت **١٥ — مربوطة بـ `TTL_OPEN`، مش رقم
حر**. لو `TTL_OPEN` اتغيّر في `index.js`، القيمة دي تتغيّر معاه في نفس التعديل.

**⚠️ الأثر مش مجرد رقم أكبر — هو تضييق مدى الاستثناء نفسه:**

| الشهر | TTL بتاعه | سلوك ضغطة المربّع بعد التعديل |
|---|---|---|
| **المفتوح** (فيه النهارده) | ١٥ دقيقة | **كاش-أولاً دايمًا.** مفتاح كاش موجود = عمره **بالتعريف** أقل من TTL بتاعه، فالـ probe عمره ما هيرجّع ≥ ١٥. يا كاش حي (نقرا منه) يا مفيش `lastUpdated` أصلاً (نسحب) — والتانية دي هي بالظبط اللي كاش-أولاً بيعملها. |
| **المقفول** (٦ ساعات / ٢٤ ساعة) | ٦ / ٢٤ ساعة | **الاستثناء زي ما هو** — شهر عمر كاشه ٢٠ دقيقة الضغط عليه بيسحبه مباشرة. |

يعني قرار **"الضغطة *هي* الزرار"** (30-08-2026) اتقيّد: على الشهر المفتوح
السحب الفوري بقى مسؤولية زرار **"تحديث أحدث شهر"** (`refreshData` →
`mode:'open'` — بيفرض السحب دايمًا بلا أي حارس). **مفيش قدرة اتفقدت**، اتنقلت
للزرار، وده أقرب للقاعدة الموحّدة ("التحديث بزرار أو تغيير فترة بس").

**⚠️ صفر `CACHE_VERSION` bump:** لا شكل الصف ولا `ORDER_QUERY` ولا أي منطق
تصنيف اتغيّر. اللي اتغيّر هو **إمتى بنطلب سحب جديد** — والمفاتيح الموجودة صالحة
زي ما هي.

**🔴 البند ده مخالف لنص مكتوب في مهارتين** — القيمة ٥ مكتوبة حرفيًا في:

| المهارة | الموضع | النص الحالي |
|---|---|---|
| `ecommoda-dashboard-builder` | `SKILL.md` Step 3-د-٣ (جدول القواعد) | «لو `lastUpdated` أقل من **٥ دقايق** → اقرا من الكاش» |
| `ecommoda-html-builder` | `references/monthly-chunk-loading.md` §9 (عيّنة الكود) | `mode: ageMin < 5 ? 'cache' : 'all'` |

التوثيق الكامل للتعديل المطلوب على المهارتين (التصنيف 🟡 مُستحسن · bump مقترح
`dashboard-builder` v3.0.0 → v3.1.0 و`html-builder` v4.0.0 → v4.1.0 · صيغة
البند الجديد) اتسلّم لأحمد في ملف منفصل بتاريخ 01-09-2026 — **لسه ما اتنفّذش
على المهارات وقت كتابة السطر ده**.

---

## 🔴 إصلاح 01-09-2026 — فلترة أطراف الفترة كانت بتوقيت UTC (`index.html` v1.12.1)

`visibleRows()` كانت بتقارن `String(r.createdAt).slice(0, 10)` — وده **تاريخ
UTC** — بحدود الفترة، وهي متولّدة في نفس الملف بـ `cairoDateStr(cairoNow())`
يعني **تاريخ قاهرة**. وكل أعمدة التاريخ المعروضة (`formatDate` ·
`formatDateTime` · `formatDateForExport`) بتعدّي على `toCairo` كمان. يعني طبقة
الفلترة كانت شغالة بتقويم مختلف عن كل اللي حواليها.

**الأثر:** كل أوردر اتعمل بين ١٢ و٣ الفجر بتوقيت القاهرة (= ٢١:٠٠–٢٤:٠٠ UTC
لليوم اللي قبله) كان بيتشال من اليوم اللي هو فيه فعلًا وينضم لليوم اللي قبله.
**مقيس على يوم واحد حقيقي (25-08-2026):** ٢٥ أوردر (`#51989` → `#52013`) من
~١٣١ ≈ **١٩٪ من أوردرات اليوم** في اليوم الغلط.

⚠️ ومداه هنا **أوسع من مجرد كارت**: `state.orders` (مخرج `visibleRows`) مقروء
في **١٩ موضع** — كروت الصفحة الرئيسية · جداول المناديب · تاب تحت التجهيز · تاب
خرج للشحن · عدّادات الأسباب · الفلاتر · و**تلات ملفات XLSX بتتصدّر منه**. يعني
الرقم الغلط كان بيخرج بره الشاشة كمان.

**الدليل (قياس مباشر على شوبيفاي، 01-09-2026):** `shop.ianaTimezone` =
`Africa/Cairo`، وفلتر `created_at:>=2026-08-25 created_at:<=2026-08-25` رجّع
أوردرات من `#51989` (`2026-08-24T21:06Z`) لحد `#52119` (`2026-08-25T20:59Z`) —
شوبيفاي بتقطع اليوم بتوقيت المتجر، والأداة بقت بتطابقها بالظبط
(`cairoDateStr(toCairo(iso))`).

**⚠️ صفر `CACHE_VERSION` bump:** الإصلاح في العرض بس — لا شكل الاستعلام ولا شكل
الصف ولا أي منطق تصنيف اتغيّر. الصفوف المتكاشة صح من الأصل؛ اللي كان غلط هو
**أنهي صف بيتعدّ في الفترة المعروضة**.

**⛔ ممنوع ترجّع `.slice(0, 10)` على `createdAt`.** ولا تنسخها من
`ecommoda-html-builder/references/monthly-chunk-loading.md` §3 — **العيّنة هناك
هي نفسها اللي كانت غلط**، والمهارة (ومعاها `ecommoda-dashboard-builder`)
محتاجة bump كاسر بالبند ده. القاعدة العامة: أي مقارنة تواريخ في الواجهة لازم
تعدّي على `cairoDateStr(toCairo(iso))`.

**⛔ `ymd()` في `index.js` مش نفس البند — متلمسهاش.** هي
`d.toISOString().slice(0, 10)` ومستخدمة في موضعين بس كـ«النهارده»
(`ttlFor` والفترة الافتراضية). القاهرة دايمًا مقدّمة على UTC، فأقصى أثر ممكن هو
TTL **أقصر** لتلات ساعات — تحفّظ مش رقم غلط. «تصليحها» بيطوّل الـ TTL على نطاق
لسه بيتغيّر = ضرر حقيقي.

⚠️ **ده إصلاح موجّه لبند 🔴 واحد، مش مراجعة كاملة على المهارات** — عشان كده
بصمة المهارات تحت **ما اتغيّرتش**. مراجعة كاملة مقابل
`ecommoda-html-builder` v2.2.0+ (تحويل `WORKER URL`/`ADMIN WORKER URL` لـ
constants — Standards Changelog #28) لسه مطلوبة عند أول تعديل حقيقي جاي.

---

## 🔴 أهم قرار في الملف ده — مفيش كاش دائم

**الأداة دي بتتبع فرع "state payload"** في `ecommoda-dashboard-builder` v2.0.0
(Step 3-أ → Step 3-ج). القاعدة القديمة (**فترة مغلقة → كاش دائم**) بقت مقصورة
على الداشبوردات اللي صفوفها بتوصف **أحداث حصلت** في الفترة — والقرار ده أصله
من الأداة دي، وهو اللي وسّع القاعدة.

**ليه الأداة مش من النوع ده:** هي بتعرض **الحالة الحالية** لأوردرات **اتعملت**
في الفترة — والحالة بتفضل تتحرك بعد ما الفترة تقفل:

```
أوردر اتعمل 15-03  وحالته النهارده Shipped
  → الأسبوع الجاي  Delivered
  → وممكن يترجع في سبتمبر
```

كاش دائم على مارس معناه إن الأوردر ده هيفضل ظاهر **"خرج للشحن" للأبد**. رقم غلط
شكله سليم، ومفيش أي خطأ بيظهر — بالظبط نوع الفشل اللي السكيل موجودة عشانه.

**TTL متدرّج بعمر الفترة، ومفيش `null` أبدًا:**

| الفترة | TTL | ليه |
|---|---|---|
| فيها النهارده | **١٥ دقيقة** | بتتغير باستمرار |
| قفلت من ≤ ٤٥ يوم | **٦ ساعات** | لسه فيها حركة تسليم وإرجاع |
| أقدم من ٤٥ يوم | **٢٤ ساعة** | الحركة نادرة — بس مش مستحيلة |

✅ **اتنقل للسكيل — `ecommoda-dashboard-builder` v2.0.0 (30-08-2026).** بقى
سؤال إلزامي في الـ Data Contract (Step 0 بند 4): *الصفوف بتوصف **أحداث حصلت** في
الفترة ولا **الحالة الحالية** لكيانات اتعملت فيها؟* — الأول بالقاعدة القديمة
(كاش دائم)، والتاني بالـ TTL المتدرّج ده. الأداة دي **مطابقة** للقاعدة الجديدة
من يوم بنائها، فمفيش عليها بند معلّق.

⚠️ **`Performance-Dashboard` مرشّح مباشر لنفس البند الكاسر** — محتاج مراجعة:
لو بيكاش فترة مقفولة بشكل دائم، أرقامه على الشهور القديمة مجمّدة دلوقتي.

**نتيجة تانية:** مؤشر الطزاجة بيعرض **أقدم** شهر في الفترة (الأكثر تحفّظًا)،
وتحته سطر بيفكّ الرقم لكل شهر — عشان "من ٦ ساعات" ما تتقريش على إنها كل البيانات
والشهر الحالي فعليًا لحظي.

---

## 🔴 قرار تاريخ بداية البيانات — `STORE_START_DATE`

**مفيش أي بيانات قبل `2026-05-01`** — تُعرض أو تُسحب — في أي مكان في الأداة.
قرار أحمد 30-08-2026: المتجر بدأ تشغيله فعليًا من التاريخ ده، فأي أوردر أقدم
(لو موجود أصلًا في شوبيفاي) مش جزء من نطاق الأداة.

**التطبيق على طبقتين، مش طبقة واحدة:**

| الطبقة | الإجراء |
|---|---|
| `index.js` (خط الدفاع الحقيقي) | `get_data`: بترفض الفترة كلها لو `dateTo < STORE_START_DATE`، وبتقصّ `dateFrom` على التاريخ ده لو أقدم منه — **قبل** أي نداء لشوبيفاي. `get_meta` بتقصّ نفس القصّ عشان مفتاح الكاش يتطابق. |
| `index.html` (منع الاختيار من الأساس) | مربعات الشهور (`computeMonthBoxes`) بتوقف عن توليد شهور أقدم من الشهر ده. حقول "من تاريخ"/"إلى تاريخ" عندهم `min="2026-05-01"` + قصّ JS إضافي في `onRangeDateChange`. |

⚠️ **القيمة معرّفة مرتين — `STORE_START_DATE` في `index.js` و`index.html` —
ولازم تتطابق دايمًا.** لو اتغيّرت (نادرًا ما يحصل، بس لو حصل)، لازم تتغيّر في
الملفين في نفس التعديل، وإلا الواجهة هتمنع اختيار فترة والـ Worker يوافق
عليها (أو العكس).

**مفيش `CACHE_VERSION` bump مطلوب** — شكل الصف نفسه ما اتغيّرش، بس مفاتيح الكاش
لفترات قبل التاريخ ده بقت ببساطة مش هتتطلب تاني (مش خطر، مجرد مفاتيح يتيمة).

---

## هيكل الكود

**`index.js`** — `§CONSTANTS` · `§CORS` · `§HELPERS` · `§SHARED` · `§ACCESS-LOG` ·
`§SHOPIFY` · `§AGGREGATE` · `§CACHE` · `§HANDLER` (`§AUTH` · `§DATA` ·
`§LOG-ENDPOINTS` · `§DIAG`)

**`index.html`** — `§STYLES` · `§SHELL` · `§LOGIN` · `§TOOL` (٦ تابات: الصفحة الرئيسية ·
تحت التجهيز · خرج للشحن · المناديب · المحافظات · الإلغاء والمرتجعات — سجل الدخول
اتلغى 30-08-2026، والتجهيز والطوابير اتقسم لتابين 30-08-2026، وتاب "المناديب
والمناطق" اتقسم لتابين 30-08-2026) · `§CONFIG` ·
`§SHELL-JS` · `§AUTH-JS` · `§DASH-JS` · `§REASONS-JS` · `§ORDERS-JS` ·
`§PREP-JS` · `§SHIPPED-JS` · `§DELEGATES-JS`

### طبقة التحميل الشهرية (`§DASH-JS`)

```
computeMonthBoxes()     → الشهر الحالي + لحد ٥ سابقين، مقصوصة على STORE_START_DATE
selectMonthBox(ym)      → شهر واحد + فورس من شوبيفاي — إلا لو كاشه أطزج من MONTH_BOX_FRESH_MINS (= TTL_OPEN)
monthChunks(from, to)   → شهور تقويمية **كاملة** (بلا قصّ)، وأول شهر بس بيتقصّ على STORE_START_DATE
visibleRows(rows, range)→ بتقصّ أطراف الفترة بـ createdAt بعد الدمج — الفترة المعروضة ما بتتغيّرش
forcedChunkIdx(chunks)  → الشهور اللي زرار "تحديث" بيسحبها (المفتوحة، أو الأحدث لو الفترة كلها في الماضي)
probeAndLoad()          → get_meta لكل شهر بالتوازي → ناقص ≤ شهر؟ حمّل : اعرض البوابة
loadData({mode})        → 'cache' | 'open' | 'all' — بتمشي شهر ورا شهر بشريط تقدّم
oldestChunkUpdate()     → طزاجة الفترة = أقدم شهر فيها
```

⚠️ **شبكة الكاش ≠ الفترة المعروضة.** الـ chunks شهور كاملة عشان المفاتيح تفضل
ثابتة ومتشاركة بين الفترات؛ اللي المستخدم شايفه هو اختياره بالظبط، و`visibleRows`
هي اللي بتوفّق بينهم. أي رجوع للقصّ بيكسر ده — Step 3-د-١ في `dashboard-builder`.

⚠️ **`selectMonthBox` بتفرض سحب مباشر، بس بعد probe لحداثة الشهر** — أقل من
`MONTH_BOX_FRESH_MINS` (**١٥ دقيقة = `TTL_OPEN`** من 01-09-2026) بتقرا من الكاش.
الاستثناء المقصود ("الضغطة *هي* الزرار") باقي **للشهور المقفولة بس** — على الشهر
المفتوح الحارس بيبتلعه بالتعريف (راجع القسم المخصص تحت). أي مصدر تغيير فترة تاني
(من/إلى تاريخ · فترة سريعة) لسه كاش-أولاً عادي عبر `probeAndLoad`.

⚠️ **البوابة بالتكلفة:** `GATE_MIN_MISSING_MONTHS = 1` — شهر واحد ناقص (وده
الوضع الطبيعي للشهر المفتوح بعد ١٥ دقيقة) بيتحمّل تلقائيًا. البوابة من شهرين
فما فوق.

⚠️ **الشهور بتتحمّل بالتسلسل مش بالتوازي** — التوازي بيولّع throttle على شوبيفاي
وبيخلي الأخطاء متشابكة، والتسلسل بيدي شريط تقدّم حقيقي.

⚠️ **شهر واحد بيفشل = التحميل كله بيفشل ومفيش أي رقم بيتعرض** — نفس قاعدة
"الصفحة الفاشلة تفشل الطلب كله" بس على مستوى الشهر. الرسالة بتسمّي الشهر
(`مايو 2026 — Shopify رجّعت HTTP 503`).

### التقسيم اللي ماينكسرش

- **الـ Worker بيملك أكواد المربعات والأسباب** (`B` / `R`) ·
  **الـ HTML بيملك أسماءها والإجراءات** (`BUCKET_META` / `UNCL_REASON_LABELS`).
- كود سبب جديد في الـ Worker من غير تسمية في `UNCL_REASON_LABELS` **بيتعرض بنصّه
  الخام** — مقصود، عشان النقص يبان بدل ما يختفي.
- أي حقل جديد في `ORDER_QUERY` → **`CACHE_VERSION` يترفع في نفس التعديل**، وإلا
  الفترات المتكاشة بترجع صفوف من غيره والمربّع الجديد يطلع صفر بلا أي خطأ.

---

## الـ endpoints

```
get_data (POST)   · get_meta (GET)   · clear_cache (POST)
get_access_logs · get_access_logs_count · get_access_logs_export   (GET — لا تاب بيقراهم في الواجهة، اتلغى 30-08-2026)
get_logs        · get_logs_count        · get_logs_export          (GET — قياسية، بترجع فاضية هنا)
check_employee · register_pin · verify_employee · log_logout · get_employees
diag · get_config
```

---

## حدود معروفة — مش أعطال

1. **مفيش أي بيانات قبل `2026-05-01`** — بداية تشغيل المتجر (قرار أحمد
   30-08-2026). سؤال "فين أوردرات مارس/أبريل" متوقّع، مش باج.
2. **Skechers · Columbia · North Face · Ecco** مش على شوبيفاي (WordPress بس) —
   مش موجودين في أي رقم في الأداة. → `woocommerce-sync-helper`.
3. **أوردرات بوسطة مالهاش `pickup_date`** إطلاقًا — الحقل بيتكتب لمناديب
   القاهرة/الجيزة بس. بتظهر تحت "(بدون تاريخ)".
4. **الأعمار محسوبة من `createdAt`** — مش من وقت دخول الحالة. الميتافيلد بيحمل
   الحالة الحالية بس. أي KPI مدة حقيقية لازم ييجي من D1
   (`tool = 'metafields_change'`) — مش منفَّذ هنا.
5. **أوردر بأكتر من دورة R/E**: المربّع بيوصف الدورة **الحالية** بس؛ الدورة
   القديمة مش ظاهرة في أي رقم (`classification-rules.md` §9-ب لسه ما اتبنتش).
   الصف بياخد `MULTI_CYCLE`.
6. **أول تحميل لفترة طويلة بياخد دقايق** — نداء بالتسلسل لكل شهر. بعد كده
   التحديث العادي = شهر واحد. حجم مفتاح الشهر ≈ ١.٥ ميجا (الحد ٢٥) — هامش مريح.
7. **أكتر من ٢٠ سطر منتج** في أوردر → `LINEITEMS_TRUNCATED` + `UNCLASSIFIED`.

---

## اختبارات اتنفّذت وقت البناء (29-08-2026)

- **٢٣ حالة تصنيف** على أمثلة حقيقية من الستور — كلها نجحت، وفيها الحالتين
  المرجعيتين: `#50091` (Rule 15 — استبدال مقفول ثم مرتجع مفتوح → `SHIPPED_RETURN`)
  و`#51496` (دورتين مفتوحتين → `MULTI_CYCLE` + `CYCLE_OVERLAP`).
- **بيانات حقيقية**: ١٢٠ أوردر من ٠١–٠٢/٠٧/٢٠٢٦ (فترة ناضجة) → صفر `UNCLASSIFIED`
  وصفر تحذيرات، ومجموع المربعات = إجمالي الأوردرات.
- **الواجهة** في متصفح فعلي (Chromium): الدخول · مؤشر الطزاجة · المربعات الستة ·
  البانرات · ٦ تابات · الرسوم · **الحالات التلاتة لأيقونة الفلتر** ·
  **الحالات التلاتة للترتيب** · الترقيم · صفر أخطاء console · وتدهور آمن لو
  Chart.js/ExcelJS ما حمّلوش (الأرقام والجداول بتفضل شغالة مع بانر واضح).
- **التحميل الشهري** في متصفح فعلي، ٦ سيناريوهات:
  ① `get_meta` بينادي لكل شهر والشهر الناقص بيتعرض **بالاسم** ·
  ② ٦ نداءات `get_data` بالتسلسل ودمج ٦٠٠ صف ·
  ③ **"تحديث الشهر الحالي" سحب شهر واحد بس من شوبيفاي** (١ من ٦) ·
  ④ "تحديث كل الشهور" سحب الستة ·
  ⑤ فترة شهر واحد في الماضي → اللافتة بتتغير لـ "تحديث أحدث شهر" وزرار "كل
  الشهور" بيختفي · ⑥ فشل شهر → رسالة باسم الشهر و**صفر مربعات ظاهرة**.

> ⚠️ **الاختبار الوحيد اللي ما اتنفّذش:** التحميل الفعلي لست شهور كاملة من
> شوبيفاي عبر الـ Worker المنشور (مفيش Worker منشور وقت البناء). الوقت وحجم
> الرد الحقيقيين لازم يتأكدوا من أول تشغيل.

## اختبارات اتنفّذت وقت تعديل 30-08-2026 (تاريخ بداية البيانات + مربعات الشهور)

- **`node --check index.js`** — الـ Worker سليم تركيبيًا.
- **الواجهة في Chromium فعلي** (بدون Worker منشور — مفيش سيرفر متاح وقت
  التعديل، زي وقت البناء الأول): صفر `pageerror`، شاشة الدخول بتفتح عادي،
  ومربعات الشهور بتتولّد صح — **٤ مربعات بس (مايو → أغسطس ٢٠٢٦)** مش ٦، عشان
  التاريخ النهارده (30-08-2026) لسه قريب من `STORE_START_DATE` — سلوك متوقّع
  ومقصود، مش نقص.
- **الفحص الآلي (Step 9 في `ecommoda-html-builder`)**: z-index (login 500 ·
  كل الـ modals 600 · toast 9999) · `.container` بيلف المحتوى فعليًا ·
  `currentEmployee` مش في localStorage · مفيش `main-seg` قديمة — كله سليم.

> ⚠️ **لسه ما اتعملش:** اختبار فعلي للضغط على مربّع شهر بعد تسجيل دخول حقيقي
> (مفيش Worker منشور ولا بيانات موظفين متاحة وقت التعديل). المنطق (`selectMonthBox`
> → `loadData({mode:'all'})`) اتراجع كود بس مش اتشغّل end-to-end.

## اختبارات اتنفّذت وقت تعديل التعديل التامن (30-08-2026)

- **`node --check index.js`** والـ JS المستخرج من `index.html` — الاتنين سليمين.
- **فحص وحدات في Chromium فعلي** على دوال التقسيم:
  `monthChunks('2026-08-24','2026-08-30')` و`('2026-08-01','2026-08-30')`
  و`('2026-08-01','2026-08-31')` → **التلاتة بيرجّعوا نفس المفتاح**
  `2026-08-01..2026-08-31` · `('2026-06-02','2026-08-30')` → ٣ شهور كاملة
  (`06-01..06-30` · `07-01..07-31` · `08-01..08-31`) و`isOpen` على أغسطس بس ·
  أول شهر مقصوص على `STORE_START_DATE` صح · `visibleRows` بتقصّ الأطراف صح.
- **فحص end-to-end في Chromium فعلي بـ Worker مموّه** (route interception —
  مش Worker منشور)، ٥ سيناريوهات كلها نجحت:
  ① شهر واحد ناقص → **مفيش بوابة**، حمّل تلقائيًا ٣١ صف ·
  ② شهرين ناقصين → **البوابة ظهرت** وصفر نداءات `get_data` ·
  ③ "آخر ٧ أيام" → نادى مفتاح **أغسطس الكامل** بـ`forceRefresh:false`
  وعرض **٧ صفوف بس** ·
  ④ مربّع شهر وكاشه دقيقتين → **حارس الحداثة منع الفورس** (قرا من الكاش) ·
  ⑤ مربّع شهر وكاشه ساعتين → **سحب مباشر** (`forceRefresh:true`).
  صفر أخطاء console (غير الشبكة المحجوبة — CDN/خطوط).

> ⚠️ **لسه ما اتعملش:** تشغيل فعلي على الـ Worker المنشور ببيانات شوبيفاي
> حقيقية. المنطق اتفحص كامل ببيانات مموّهة بس.

---

## اختبارات اتنفّذت وقت التعديل العاشر (31-08-2026 — المحافظة بدل المدينة)

- **`node --check`** على `index.js` وعلى الـ JS المستخرج من `index.html` — الاتنين سليمين.
- **الفحص الآلي الإلزامي** (`ecommoda-html-builder` Step 9): z-index (login 500 · كل
  الـ modals 600 · toast 9999 · sticky 300) · `settingsModal` = 0 · `main-seg` = 0 ·
  `ar-EG` = 0 · `⇅` ثابتة = 0 · `.container` بيلف المحتوى · `--container-max: 1400px`
  (Tier L) · سطر بصمة المهارات = 1 — كله سليم.
- **فحص فعلي في Chromium headless** ببيانات تركيبية (١١٢ أوردر على ٢٨ قيمة محافظة —
  الـ ٢٧ + `null` — عبر ١٤ bucket)، وكله نجح:
  ① أسماء التابين الجداد ظهروا في الزرار **وفي الشيب والقائمة المنسدلة** (بيقروا من
  نص الزرار، فاتحدّثوا لوحدهم) ·
  ② عمود "المحافظة" موجود في الأربع جداول التفصيلية (`ordersTable` 9 ·
  `delegateOrdersTable` 8 · `prepOrdersTable` 6 · `shippedOrdersTable` 5) وبيعرض
  القيمة الخام صح ·
  ③ **`provinceBody` رجّع ٢٨ صف** — لو السقف القديم لسه موجود كان هيرجّع ٢٥ ·
  ④ فلتر المحافظة عبر `msToggleItem` الحقيقي: القايمة اتبنت بأسماء صح
  (`Cairo(5)` … `(بدون محافظة)(5)`)، الشيب طلع **"المحافظة: Red Sea"**، الزرار
  اتسمّى بالقيمة، أيقونة الفلتر اتلوّنت، والجدول رجّع صفوف المحافظة دي بس ·
  ⑤ `(بدون محافظة)` رجّع صفوف الـ `null` بس والشيب عرض الاسم العربي مش `__none` ·
  ⑥ الترتيب على العمود شغّال في التابات (`toggleSort`/`toggleSortPrep`/
  `toggleSortShipped`) — الـ nulls الأول تصاعديًا زي باقي الأعمدة النصية ·
  ⑦ "مسح كل الفلاتر" رجّع كل الصفوف · ⑧ عناوين الرسمين والتصدير اتحدّثوا.
  **صفر أخطاء console/pageerror** (غير الشبكة المحجوبة — CDN/خطوط).

> ⚠️ **لسه ما اتعملش:** تشغيل على الـ Worker المنشور ببيانات شوبيفاي حقيقية —
> تحديدًا التأكد إن قيم `province` الفعلية في الستور نضيفة (مش فاضية بالجملة على
> أوردرات قديمة اتعملت قبل ما الخانة تبقى إجبارية). لو طلع فيه نسبة كبيرة
> `(بدون محافظة)`، ده **بيانات مش كود**.

---

## بصمة المهارات

| المهارة | الإصدار وقت آخر تعديل |
|---|---|
| ecommoda-worker-builder | v1.1.0 |
| ecommoda-html-builder | v2.1.0 |
| ecommoda-dashboard-builder | v2.1.0 |
| ecommoda-order-lifecycle | v1.1.0 |
| ecommoda-constants | v1.2.0 |

آخر مطابقة: 31-08-2026 · `index.js` v1.1.2 · `index.html` v1.12.0

> ⚠️ **البصمة عمدًا ما اتحرّكتش مع تعديلَي 01-09-2026** (`index.html` v1.12.1
> و v1.12.2): الاتنين تطبيق بند موجّه واحد لكل منهم، مش مراجعة كاملة للأداة دي
> مقابل الإصدارات الحالية. تتحدّث عند أول مراجعة كاملة.
>
> ⚠️ وتعديل **v1.12.2** حالة خاصة في الاتجاه العكسي: الأداة دلوقتي **مقدّمة**
> على المهارتين في بند حارس مربّع الشهر (١٥ بدل ٥) — البند اتوثّق للنقل
> للمهارات في ملف منفصل 01-09-2026 ولسه ما اتنفّذش عليها. لحد ما ينتقل، أي
> مراجعة للأداة مقابل المهارة هتشوف السطر ده انحراف — **هو مقصود**.

🔴 معلّقة (١): **مراجعة كاملة مقابل `ecommoda-html-builder` v4.x و
`ecommoda-dashboard-builder` v3.x** — الأداة متأخرة بإصدارين كاسرين. أبرز بندين
معروفين: (أ) `WORKER URL` و`ADMIN WORKER URL` المفروض يبقوا constants في
`§CONFIG` بدل حقول إعدادات + `localStorage` (Standards Changelog #28)؛
(ب) `MIN_WORKER_VERSION` + `cmpVersion()` بدل المقارنة الحرفية
`workerVersion !== TOOL_VERSION` — دي السبب المباشر في إن تحذير "نسخة الـ Worker
مختلفة" مولّع على طول في الأداة دلوقتي (`index.html:2161`)، لأن رقمي الواجهة
والـ Worker **مستقلين بالتصميم**.

🔴 معلّقة (٢): **نقل بند حارس مربّع الشهر للمهارتين** — 🟡 مُستحسن، bump مقترح
`dashboard-builder` v3.0.0 → v3.1.0 و`html-builder` v4.0.0 → v4.1.0. التفاصيل
في ملف التوثيق المسلّم 01-09-2026.

آخر تحديث: 01-09-2026

</div>
