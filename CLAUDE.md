<div dir="rtl" style="text-align: right;">

# Logistics-Control-Center — سياق الأداة

![version](https://img.shields.io/badge/version-v1.7.0-blue)

> الملف ده بيتحمّل تلقائيًا في أي جلسة على الريبو ده. القواعد المشتركة في السكيلز —
> هنا سياق الأداة دي بس.

---

## إيه هي

داشبورد **قراءة فقط** لمسئول الشحن: حالات الأوردرات إحصائيًا وتفصيليًا من
**01-05-2026** (بداية تشغيل المتجر) وحتى النهارده، مع المناديب والمناطق
والمدن، طابور التجهيز (الطباعة والتغليف)، مواعيد الاستلام، أسباب الإلغاء
والمرتجع، والملاحظات.

**مفيش أي mutation على شوبيفاي في الأداة دي.** أي طلب "خلّيها تعدّل حالة" =
أداة تانية، مش تعديل هنا.

**مفيش أي بيانات قبل 01-05-2026** — راجع "🔴 قرار تاريخ بداية البيانات" تحت.

---

## الهوية والثوابت

| | |
|---|---|
| الريبو | `ecommoda-dev/Logistics-Control-Center` |
| Worker | `logistics-control-center-worker` |
| الواجهة | `https://ecommoda-dev.github.io/Logistics-Control-Center/` |
| `TOOL_NAME` (D1) | `logistics_control_center` |
| قيم `type` | `login` · `logout` **فقط** |
| `TOOL_VERSION` (HTML) | `v1.7.0` |
| `WORKER_VERSION` | `v1.1.0` |
| `CACHE_VERSION` | `v1` |
| `STORE_START_DATE` | `2026-05-01` — معرّفة في `index.js` **و** `index.html`، لازم تتطابق |
| ربط D1 | `DB` → `ecommoda-dev-logs` |
| ربط KV | `DASH_KV` → namespace خاص بالأداة |
| CORS | Option A (wildcard) — أداة قراءة فقط |
| Container Tier | **L** (`1400px`) — داشبورد بمربعات + رسوم + جدول كثيف |
| تسجيل الدخول | 🔒 **مطلوب** — Universal D1 Auth (قرار أحمد 29-08-2026: الشاشة بتعرض عناوين تفصيلية وأسماء مناديب) |

> ⚠️ **`logistics_control_center` لازم تتسجّل في `ecommoda-constants` §7 قبل أول
> `writeLog`** — يعني قبل أول تسجيل دخول فعلي على الأداة. الصف المطلوب:
> `| Logistics Control Center | logistics_control_center | login · logout |`

---

## القرارات اللي اتحسمت وقت البناء (29-08-2026)

| القرار | الاختيار | ليه |
|---|---|---|
| **G-11** — `S1 = Returned` و `cancelledAt` فاضي | **`UNCLASSIFIED`** (قراءة Performance-Dashboard) | الخسارة تيجي من حدث شوبيفاي مش من ميتافيلد. الصف بياخد `S1_RETURNED_NO_CANCEL` + الإجراء (Cancel + Restock). **الأداتين دلوقتي بيقولوا نفس الرقم.** |
| الفلوس | **مستبعدة تمامًا** | سؤال مسئول الشحن تشغيلي مش مالي، والمرتجع الجزئي/الاستبدال محتاجين حساب على مستوى القطعة (`piece-level-valuation.md`) — خارج نطاق الأداة. |
| **تحميل الفترة** | **شهر بشهر** — الواجهة بتقسّم الفترة على حدود الشهور وبتنادي `get_data` مرة لكل شهر بالتسلسل مع شريط تقدّم | أحمد اختار "طلب واحد" الأول وغيّر رأيه (29-08). النتيجة: زرار التحديث بيسحب الشهر اللي لسه بيتغيّر بس (~٣ آلاف أوردر) بدل الست شهور (~٢٠ ألف)، وحجم كل مفتاح كاش بقى ~١.٥ ميجا بدل ٨–٩. |
| **كاش دائم** | 🔴 **ملغي تمامًا — TTL متدرّج بدل منه** | تفصيله تحت في "أهم قرار في الملف ده". |
| توقّع اكتمال الفترة (Rule 11) | **مش منفَّذ** | ما اتطلبش. لو اتطلب لاحقًا، `dashboard-builder` Rule 11 إلزامية بالكامل — معدلات مقاسة، حساب على مستوى الـ sub-bucket، ورجل المرتجع تتحسب خسارة. |
| فلاتر تاب "كل الأوردرات" | **بتأثّر على جدول التاب ده بس** | فلتر مخفي بيغيّر مربّع في تاب تاني = سكرين شوت لرقم غلط شكله سليم. |

## القرارات اللي اتحسمت بعد أول تسليم (30-08-2026)

| القرار | الاختيار | ليه |
|---|---|---|
| **تاريخ بداية البيانات** | 🔴 **`STORE_START_DATE = '2026-05-01'`** — مفيش بيانات قبله في أي مكان | قرار أحمد: بداية تشغيل المتجر فعليًا. تفصيله تحت في "🔴 قرار تاريخ بداية البيانات". |
| فلتر الفترة السريعة (تاب النظرة العامة) | **اتلغى بالكامل** — بدّله **٦ مربعات شهور** (الشهر الحالي + لحد ٥ سابقين، مقصوصة على `STORE_START_DATE`) | طلب صريح من صاحب الأداة. الضغط على مربّع = فلترة الشاشة كلها على الشهر ده. حقول "من/إلى تاريخ" لسه موجودة لفترة مخصّصة. |
| تحديث المربعات | **كل ضغطة على مربّع شهر بتسحب من شوبيفاي مباشرة، مش كاش-أولاً** | طلب صريح من صاحب الأداة — استثناء مقصود من قاعدة "التحديث بزرار أو تغيير فترة بس" في `dashboard-builder` (الضغطة على مربّع الشهر *هي* الزرار). التحميل الأول بعد الدخول لسه كاش-أولاً عادي. |
| جدول المقارنة الشهرية | 🔴 **اتلغى بالكامل** — مفيش بديل Δ حاليًا | طلب صريح من صاحب الأداة. القسم كان بيغطّي غياب Δ% على المربعات (راجع نسخة v1.0.0 من الملف ده) — الغياب ده رجع تاني من غير بديل. |
| دليل أسباب المراجعة | **Collapsible (`<details>`)، مقفول افتراضيًا** | طلب صريح من صاحب الأداة — القسم كان بياخد مساحة دايمة حتى لو مفيش صفوف تحتاج مراجعة كتير. |
| تاب "سجل الدخول" | 🔴 **اتلغى من الواجهة بالكامل** | طلب صريح من صاحب الأداة. `get_access_logs`/`_count`/`_export` **لسه موجودين في الـ Worker** (مفيش داعي نمسحهم — دخول/خروج لسه بيتسجّل في D1)، بس **مفيش تاب بيقراهم**. لو اتطلب رجوعهم لاحقًا، الـ endpoints جاهزة. |
| تاب "كل الأوردرات" | 🔴 **اتلغى — جدول الأوردرات اندمج في تاب "نظرة عامة"** (بعد قسم "دليل أسباب المراجعة"، آخر الصفحة) بنفس الفلاتر والترتيب وتصدير XLSX | طلب صريح من صاحب الأداة. فلاتر الجدول لسه بتأثّر على الجدول نفسه بس، مش على مربعات/رسوم "نظرة عامة" فوقه. |
| عمودا "سبب الإلغاء" و"سبب المرتجع" في جدول الأوردرات | 🔴 **اتشالوا من الجدول** | طلب صريح من صاحب الأداة. البحث النصي في الجدول لسه بيدوّر في السببين، بس مش معروضين كعمود منفصل. |
| تاب "التجهيز والطوابير" | 🔴 **اتقسم لتابين: 📦 تحت التجهيز و🚛 خرج للشحن** | طلب صريح من صاحب الأداة. "خرج للشحن" = مواعيد الاستلام. كل تاب فيه جدول تفصيلي مستقل (فلاتر اختيار متعدد + ترتيب + تصدير XLSX) زي جدول "نظرة عامة"، بمنطق فلترة مستقل تمامًا (`msStatePrep`/`msStateShipped`) عن جدول "نظرة عامة" (`msState`). |
| نطاق تاب "تحت التجهيز" (تعديل 30-08-2026) | 🔴 **بقى مطابق لمربّع `PREP` في `classifyOrder()`** (`PREP_CONFIRMED` + `PREP_EXCHANGE` + `PREP_RETURN`) — مش `S1 = Ready` بس زي الأول | قرار أحمد: التاب كان بيفلتر `state.orders` على `r.s1 === 'Ready'` مباشرة (تجاهل تام لـ `r.bucket`/S2)، فرقم كارت "أوردرات Ready" فيه (233) كان مختلف عن رقم كارت "تحت التجهيز" في نظرة عامة (258) — نفس التاريخ، نفس الفترة. الفرق: أوردرات لسه Confirmed/Confirmed+Edit (ما وصلتش Ready)، وأوردرات Delivered فيها دورة استبدال/مرتجع لسه في مرحلة التجهيز. `prepBaseRows()` بقت `bucketGroup(r.bucket) === 'PREP'`، وجدول التاب بقى فيه عمود "المرحلة" (`bucketLabel(r.bucket)`) يوضّح نوع كل صف. كارت "محاولة تسليم متكررة" بقى مقصور على `PREP_CONFIRMED` بس (عن طريق `isRepeatAttempt()`) — أوردرات `PREP_EXCHANGE`/`PREP_RETURN` أصلًا Delivered فـ FULFILLED عندها طبيعي، مش علامة تسليم متكرر. طابور الحالات المؤقتة (`TEMP_S1`) فوق الكروت متأثّرش — لسه بيفلتر بـ S1 مباشرة، مفهوم منفصل عن نطاق الجدول الرئيسي. |
| مربعات KPI في "تحت التجهيز"/"خرج للشحن" | **قابلة للضغط كفلتر سريع على الجدول تحتها** | طلب صريح من صاحب الأداة. الضغط على مربّع بيفلتر جدول التاب على أوردرات المربّع ده بس؛ ضغطة تانية على نفس المربّع بترجّع كل الصفوف. مربعات "نظرة عامة" **الكبيرة** فوق لسه مش قابلة للضغط — ⚠️ **حُدِّث 30-08-2026: المربعات الفرعية (bd-item) جوّه كروت "نظرة عامة" بقت قابلة للضغط** (راجع الجدول تحت). |
| سكرول الجداول | **سكرول رأسي مستقل داخل كل جدول** (`max-height` + `overflow-y:auto` على `.tbl-scroll`)، هيدر الأعمدة `position:sticky` | طلب صريح من صاحب الأداة، بالقياس على أداة تانية شافها. اتطبّق على كل جدول في الأداة (`.data-table` جوّه `.tbl-scroll`) مش بس التابين الجداد. |
| أسامي المناطق في العرض | **`Cairo+Giza` → "قاهرة وجيزة" و`Other_Regions` → "محافظات"** — تعريب واجهة بس، القيمة الخام في `custom.zone` متغيّرتش | طلب صريح من صاحب الأداة. مطبّق عن طريق `zoneLabel()` في كل مكان بيتعرض فيه اسم المنطقة (جداول، فلاتر، رسوم، تصدير XLSX) — قيم تانية زي `Show_Room` بتفضل زي ما هي (مش مطلوب تغييرها). |

## القرارات اللي اتحسمت في التعديل التالت (30-08-2026 — كروت وجداول التجهيز/الشحن)

| القرار | الاختيار | ليه |
|---|---|---|
| كروت "تحت التجهيز" | **بقى فيها نفس المربعات الفرعية (مؤكد/استبدال/مرتجع) اللي في كروت "نظرة عامة"** — تحت زرار "عرض التفاصيل" لكل كارت | طلب صريح من صاحب الأداة. المربعات الفرعية محسوبة على نطاق كل كارت (كل أوردرات التجهيز / لسه ما اتطبعش / اتطبع وما اتغلّفش / اتغلّف) مش على `prepBaseRows()` كله. |
| زرار "عرض التفاصيل" | **مفتاح واحد (`breakdownsOpen`) لكل الكروت في الأداة** — ضغطة على أي زرار بتفتح/تقفل كل الكروت اللي ليها تفاصيل فرعية مرة واحدة | طلب صريح من صاحب الأداة، قياسًا على Performance-Dashboard. قبل كده كان كل كارت له مفتاح فتح/قفل مستقل (`openBreakdowns` Set بمفتاح الكارت). |
| المربعات الفرعية (bd-item) | **قابلة للضغط زي المربعات الكبيرة — بتفلتر الجدول تحتها** | طلب صريح من صاحب الأداة، قياسًا على Performance-Dashboard. **النطاق:** تاب "نظرة عامة" (بتفلتر `msState.bucket`/`msState.finality`) وتاب "تحت التجهيز" (بتفلتر `msStatePrep.bucket`) — التابين دول بس عندهم مربعات فرعية أصلًا. تاب "خرج للشحن" مالوش مربعات فرعية (الأنواع التلاتة أصلًا كروت مستقلة قابلة للضغط)، وتاب "الإلغاء والمرتجعات" مربعاته بسيطة من غير تفاصيل فرعية أو جدول مفلتر بنفس المنطق — فمفيش عليهم تعديل. |
| كارت + عمود "محاولة تسليم متكررة" (تاب تحت التجهيز) | 🔴 **اتشالوا بالكامل** | طلب صريح من صاحب الأداة. `isRepeatAttempt()` والمنطق المرتبط بيه اتمسحوا من الكود بالكامل (مش مجرد إخفاء). |
| تصنيف "لسه ما اتطبعش" / "اتغلّف" (تاب تحت التجهيز) | 🔴 **تصحيح ازدواج عدّ** — أوردر عليه `packedAt` من غير `printedAt` كان بيتحسب في الكارتين مع بعض. دلوقتي "لسه ما اتطبعش" = مفيش طباعة **ولا** تغليف، و"اتغلّف" = عليه `packedAt` (بما فيهم الشذوذ ده) مع ملحوظة صريحة "تم التغليف بدون طباعة" على الكارت والصف في الجدول | قرار أحمد — الشذوذ التشغيلي (تغليف قبل الطباعة) لازم يبان في مكان واحد واضح، مش يختفي في عدّ مزدوج. |
| فلتر "دورة التجهيز" (تاب تحت التجهيز) | 🔴 **اتسمّى "مرحلة التجهيز"** | طلب صريح من صاحب الأداة. عمود "المرحلة" في نفس الجدول متغيّرش (اسم مختلف أصلًا). |
| ترتيب أعمدة جدولي "تحت التجهيز"/"خرج للشحن" | **كل الأعمدة بقت قابلة للترتيب** (المرحلة/الحالة · عمر الأوردر · اتطبع/اتغلّف · الملاحظات · وسم المراجعة) | طلب صريح من صاحب الأداة — كانت أعمدة نصية/محسوبة زي "عمر الأوردر" و"الملاحظات" بلا ترتيب. |
| جدول "مواعيد الاستلام" وكارت "بدون تاريخ استلام" (تاب خرج للشحن) | 🔴 **اتشالوا بالكامل** | طلب صريح من صاحب الأداة. عمود "تاريخ الاستلام" لسه موجود وقابل للترتيب في جدول الأوردرات نفسه — مفيش فقدان بيانات، بس القسم المجمّع المنفصل اتلغى. |

## تصحيح بعد أول استخدام للمربعات الفرعية (30-08-2026 — v1.5.1)

المربعات الفرعية اللي اتضافت في التعديل فوق كان فيها باج: كود الـ bucket نفسه
(`PREP_CONFIRMED`/`PREP_EXCHANGE`/`PREP_RETURN`) متكرر في الأربع كروت في تاب
"تحت التجهيز"، والفلتر القديم كان بيتفعّل على كود الـ bucket لوحده — فالضغط
على "استبدال" في أي كارت كان بيضيء "استبدال" في **الأربع كروت مع بعض**، وضغط
مربّع فرعي في كارت بعد ما كارت كبير تاني كان متفلتر كان بيرجّع صفوف فاضية
(الفلتر القديم `prepQuick` فاضل عالق على الكارت الأول).

**الحل:** `prepBdClick(cardKey, bucket)` بقى بياخد هوية الكارت مع كود الـ
bucket مع بعض، وبيحدّث `prepQuick` **و** `msStatePrep.bucket` سوا — فكل مربّع
فرعي بيضيء مربّعه بس (مش نظيره في كارت تاني)، وبيفلتر الجدول على تقاطع الكارت
+ النوع الصح. الضغط على أي كارت كبير (`prepQuickToggle`) بقى كمان بيمسح أي
فلتر bucket متسيّب من مربّع فرعي في كارت تاني. اتفحص فعليًا في Chromium
headless بأوردرات تركيبية تغطي التقاطعات الأربعة (notPrinted/printedNotPacked/
packed × كل نوع) — كل تقاطع بيرجّع صفوفه الصح بس.

## القرارات اللي اتحسمت في التعديل الرابع (30-08-2026 — تقسيم تاب المناديب والمناطق)

| القرار | الاختيار | ليه |
|---|---|---|
| تاب "المناديب والمناطق" | 🔴 **اتقسم لتابين مستقلين: 🚚 المناديب و🗺️ المناطق** | طلب صريح من صاحب الأداة. رسم "أوردرات كل مندوب" وجدولا "المناديب" الجداد راحوا لتاب `panelDelegates` (`renderDelegatesTab`)؛ رسم "توزيع المناطق" وجدولا "أداء المناطق"/"أعلى 25 مدينة" فضلوا زي ما هم في `panelRegions` (`renderRegionsTab`) — بلا تعديل على منطقهم. |
| ترتيب التابات | **نظرة عامة · تحت التجهيز · خرج للشحن · المناديب · المناطق · الإلغاء والمرتجعات** | طلب صريح من صاحب الأداة. `TAB_IDS` اتحدّث بنفس الترتيب — ترتيب التابات في الواجهة والقائمة المنسدلة (`buildTabChipMenu`) بيتبع ترتيب المصفوفة دي، مش ترتيب الـ `<div class="tab-panel">` في الـ DOM (كل التابات `display:none` إلا النشط، فترتيب الـ DOM مالوش تأثير بصري). |
| جدول "أداء المناديب" (تاب المناديب) | 🔴 **اتشال بالكامل** — بدّله جدولين لكل مندوب من **زاويتين** | طلب صريح من صاحب الأداة: "احصائيات بكل مندوب من زاويتين — لو الأوردر لسه انتقالي ولو حالة نهائية، والتركيز أكتر على الانتقالي عشان مسئول الشحن يعرف الأوردرات اللي لسه متقفلتش وتفاصيلها ايه". |
| **زاوية ١ — جدول "أوردرات لسه متقفلتش"** (`delegateTransitTable`) | عمود لكل bucket انتقالي (`PENDING_CONFIRM` · `PREP_CONFIRMED/EXCHANGE/RETURN` · `SHIPPED_CONFIRMED/EXCHANGE/RETURN` · `UNCLASSIFIED`) + الإجمالي الانتقالي + **أقدم أوردر لسه مفتوح** (لينك + عمره، ملوّن أصفر/أحمر زي طابور الحالات المؤقتة) + **متوسط العمر** | التركيز على الانتقالي (طلب أحمد) بيتحقق بعمودي العمر — دول مش موجودين في جدول الزاوية التانية. `aggregateDelegateStage(rows, DELEGATE_TRANSIT_BUCKETS, true)` — بارامتر `withAge=true`. |
| **زاوية ٢ — جدول "الأوضاع النهائية"** (`delegateFinalTable`) | عمود لكل bucket نهائي (`DELIVERY_BASIC/EXCHANGE/PARTIAL_RETURN` · `LOST_CANCELLED/RTO/FULL_RETURN`) + الإجمالي النهائي + نسبة التسليم/الرفض (نفس معادلة `aggregateBy` القديمة) | بديل مباشر للجدول القديم المحذوف، بس بتفصيل أدق (٣ أنواع تسليم منفصلة بدل رقم "تسليمات" واحد). `aggregateDelegateStage(rows, DELEGATE_FINAL_BUCKETS, false)`. |
| كل delegate بيظهر في الجدولين حتى لو صفر | **مقصود** | صف بصفر في الزاوية الانتقالية معناه "كل أوردرات المندوب ده مقفولة" — إشارة مفيدة لمسئول الشحن، مش سطر ناقص. |
| اسم مربّع `PREP_CONFIRMED` | 🔴 **"مؤكد / تحت التجهيز" → "أساسي / تحت التجهيز"** | طلب صريح من صاحب الأداة، في كل مكان (`BUCKET_META`، فبيتوّرث تلقائيًا لكل الكروت والجداول والفلاتر اللي بتستخدم `bucketLabel()`). |
| اسم مربّع `SHIPPED_CONFIRMED` | 🔴 **"مؤكد / خرج للشحن" → "أساسي / خرج للشحن"** | نفس السبب فوق. |
| اسم مربّع/كارت `LOST` | 🔴 **"خسائر" → "إلغاءات + مرتجعات"** | طلب صريح من صاحب الأداة، في كل مكان (`GROUP_META.LOST.label` + legend رسم تاب "المناديب" اللي كان بيكتب "خسائر" ثابتة بدل ما يقرا من `GROUP_META`). |

## القرارات اللي اتحسمت في التعديل الخامس (30-08-2026 — كروت المناديب + تبسيط الجداول + دمج جداول المناطق)

| القرار | الاختيار | ليه |
|---|---|---|
| رسم "أوردرات كل مندوب (أعلى ١٠)" (تاب المناديب) | 🔴 **اتشال بالكامل** — بدّله كارت ملخص لكل مندوب | طلب صريح من صاحب الأداة. الرسم كان مقصور على أعلى ١٠ مناديب بس؛ الكارت الجديد بيغطي كل المناديب. |
| كارت ملخص المندوب (`renderDelegateCards` بيستخدم `aggregateBy(rows, r => r.courier)`) | إجمالي · تسليم · إلغاء/مرتجع (`rto+fullReturn+cancelled`) · نسبة تسليم بشريط تقدّم ملوّن — **بلا شريط لنسبة المرتجعات** | طلب صريح من صاحب الأداة، بالقياس على سكرين شوت من أداة تانية. الكروت **قابلة للضغط** (`delegateCardClick`) — بتفلتر جدولي التاب تحتها (`delegateQuick`) على المندوب المختار، زي باقي كروت الأداة (`prepQuickToggle`/`overviewBdClick`). ده كمان الرد على "فين الفلاتر؟" — التابين مكانش فيهم أي آلية فلترة/تفاعل قبل كده، عكس باقي التابات. |
| أعمدة جدولي "المناديب" (الانتقالي والنهائي) | 🔴 **الأعمدة الفرعية التفصيلية (PREP_CONFIRMED/EXCHANGE/RETURN، DELIVERY_BASIC/EXCHANGE/PARTIAL_RETURN، إلخ) اتستبدلت بأعمدة المربعات الأساسية** (تحت التأكيد/تحت التجهيز/خرج للشحن/خارج التصنيف/تسليمات/إلغاءات + مرتجعات — نفس `GROUP_META`) | طلب صريح من صاحب الأداة. التفصيل الفرعي لسه محسوب داخليًا (`withGroupTotals` بيجمّع الأعمدة الفرعية لأعمدة أساسية) لأغراض نسب التسليم/الرفض، بس مش معروض كعمود منفصل — التفصيل الفردي (مؤكد/استبدال/مرتجع لكل حالة) بقى غير متاح من الجدولين، الكارت مبيوضحوش هو تفصيل تاني (إجمالي/تسليم/إلغاء-مرتجع). |
| عمود "متوسط العمر" (جدول "أوردرات في مرحلة انتقالية") | 🔴 **اتشال بالكامل** | طلب صريح من صاحب الأداة. عمود "أقدم أوردر لسه مفتوح" فضل زي ما هو. |
| حجم خط الأرقام في جدولي المناديب | **كبّر شوية** (`.dt-lg-num` — 15px بدل 12.5px الافتراضي) | طلب صريح من صاحب الأداة. |
| عنوان "أوردرات لسه متقفلتش" | 🔴 **"أوردرات في مرحلة انتقالية"** | طلب صريح من صاحب الأداة (إعادة تسمية بس — نفس الجدول ونفس المنطق). |
| عنوان "الأوضاع النهائية" | 🔴 **"أوردرات ذات حالة نهائية"** | نفس السبب فوق. |
| جدولا "أداء المناطق"/"أعلى ٢٥ مدينة" (تاب المناطق) | 🔴 **نفس تجربة جدولي "المناديب" (أعمدة المربعات الأساسية + أقدم أوردر لسه مفتوح + نسب التسليم/الرفض) بس مدموجين في جدول واحد** بدل جدولين منفصلين (انتقالي/نهائي) — `aggregateEntityMerged(rows, keyFn)` | طلب صريح من صاحب الأداة: "نقل نفس تجربة جداول حالة كل مندوب... يتم دمج الجدولين". دالة `aggregateEntityMerged` عامة بـ `keyFn` — مستخدمة لكل من `r => r.zone` و`r => r.city`؛ الإجمالي = مجموع الإجمالي الانتقالي + النهائي (يغطي كل الـ ١٤ bucket، زي `aggregateBy` القديمة بالظبط). |
| رسوم بيانية إضافية للمدينة (تاب المناطق) | 🆕 **رسمين جداد**: "توزيع الأوردرات لأعلى ١٥ مدينة" (عمود مكدّس تسليمات/في الطريق/إلغاءات+مرتجعات — نفس تصميم الرسم المحذوف من تاب المناديب) و"نسبة التسليم لأعلى ١٥ مدينة" (عمود أفقي ملوّن بالعتبات المعتادة ٨٠٪/٦٠٪) | طلب صريح من صاحب الأداة — مكانش فيه أي رسم للمدينة قبل كده، غير جدول "أعلى ٢٥ مدينة". رسم "توزيع المناطق" (Donut) اتسيب زي ما هو. |
| `aggregateDelegateStage(rows, bucketList, withAge, keyFn)` | بقت بتاخد `keyFn` اختياري (افتراضيًا `r => r.courier`) بدل `r.courier` مكتوبة جوّاها | ضروري عشان تتستخدم من `aggregateEntityMerged` بمفتاح زون/مدينة كمان — من غيره التجميع كان هيفضل بيتجمّع بالمندوب حتى لو الجدول بيعرض مناطق/مدن. |

**اختبار:** `node --check` على الـ JS المستخرج من الملف — سليم. فحص فعلي في Chromium headless
ببيانات تركيبية (مناديب/مناطق/مدن متعددة عبر الـ ١٤ bucket): كروت المناديب اتعرضت
صح، الضغط على كارت فلتر الجدولين على مندوب واحد بس وضغطة تانية رجّعت كل الصفوف،
عدد أعمدة كل جدول طابق عدد الخلايا في كل صف (٧/٦ لجدولي المناديب، ١١ لجدولي
المناطق/المدن المدموجين)، والعنوانين الجداد ("أوردرات في مرحلة انتقالية"/"أوردرات
ذات حالة نهائية") ظهروا صح. الرسوم البيانية اتفحصت بالتدهور الآمن بس (Chart.js
ما حمّلش في بيئة الفحص المعزولة) — لسه محتاجة تأكيد بصري على نشر حقيقي.

---

## 🔴 أهم قرار في الملف ده — مفيش كاش دائم

**الأداة دي بتتبع فرع "state payload"** في `ecommoda-dashboard-builder` v2.0.0
(Step 3-أ → Step 3-ج). القاعدة القديمة (**فترة مغلقة → كاش دائم**) بقت مقصورة
على الداشبوردات اللي صفوفها بتوصف **أحداث حصلت** في الفترة — والقرار ده أصله
من الأداة دي، وهو اللي وسّع القاعدة.

**ليه الأداة مش من النوع ده:** هي بتعرض **الحالة الحالية** لأوردرات **اتعملت**
في الفترة — والحالة بتفضل تتحرك بعد ما الفترة تقفل:

```
أوردر اتعمل 15-03  وحالته النهارده Shipped
  → الأسبوع الجاي  Delivered
  → وممكن يترجع في سبتمبر
```

كاش دائم على مارس معناه إن الأوردر ده هيفضل ظاهر **"خرج للشحن" للأبد**. رقم غلط
شكله سليم، ومفيش أي خطأ بيظهر — بالظبط نوع الفشل اللي السكيل موجودة عشانه.

**TTL متدرّج بعمر الفترة، ومفيش `null` أبدًا:**

| الفترة | TTL | ليه |
|---|---|---|
| فيها النهارده | **١٥ دقيقة** | بتتغير باستمرار |
| قفلت من ≤ ٤٥ يوم | **٦ ساعات** | لسه فيها حركة تسليم وإرجاع |
| أقدم من ٤٥ يوم | **٢٤ ساعة** | الحركة نادرة — بس مش مستحيلة |

✅ **اتنقل للسكيل — `ecommoda-dashboard-builder` v2.0.0 (30-08-2026).** بقى
سؤال إلزامي في الـ Data Contract (Step 0 بند 4): *الصفوف بتوصف **أحداث حصلت** في
الفترة ولا **الحالة الحالية** لكيانات اتعملت فيها؟* — الأول بالقاعدة القديمة
(كاش دائم)، والتاني بالـ TTL المتدرّج ده. الأداة دي **مطابقة** للقاعدة الجديدة
من يوم بنائها، فمفيش عليها بند معلّق.

⚠️ **`Performance-Dashboard` مرشّح مباشر لنفس البند الكاسر** — محتاج مراجعة:
لو بيكاش فترة مقفولة بشكل دائم، أرقامه على الشهور القديمة مجمّدة دلوقتي.

**نتيجة تانية:** مؤشر الطزاجة بيعرض **أقدم** شهر في الفترة (الأكثر تحفّظًا)،
وتحته سطر بيفكّ الرقم لكل شهر — عشان "من ٦ ساعات" ما تتقريش على إنها كل البيانات
والشهر الحالي فعليًا لحظي.

---

## 🔴 قرار تاريخ بداية البيانات — `STORE_START_DATE`

**مفيش أي بيانات قبل `2026-05-01`** — تُعرض أو تُسحب — في أي مكان في الأداة.
قرار أحمد 30-08-2026: المتجر بدأ تشغيله فعليًا من التاريخ ده، فأي أوردر أقدم
(لو موجود أصلًا في شوبيفاي) مش جزء من نطاق الأداة.

**التطبيق على طبقتين، مش طبقة واحدة:**

| الطبقة | الإجراء |
|---|---|
| `index.js` (خط الدفاع الحقيقي) | `get_data`: بترفض الفترة كلها لو `dateTo < STORE_START_DATE`، وبتقصّ `dateFrom` على التاريخ ده لو أقدم منه — **قبل** أي نداء لشوبيفاي. `get_meta` بتقصّ نفس القصّ عشان مفتاح الكاش يتطابق. |
| `index.html` (منع الاختيار من الأساس) | مربعات الشهور (`computeMonthBoxes`) بتوقف عن توليد شهور أقدم من الشهر ده. حقول "من تاريخ"/"إلى تاريخ" عندهم `min="2026-05-01"` + قصّ JS إضافي في `onRangeDateChange`. |

⚠️ **القيمة معرّفة مرتين — `STORE_START_DATE` في `index.js` و`index.html` —
ولازم تتطابق دايمًا.** لو اتغيّرت (نادرًا ما يحصل، بس لو حصل)، لازم تتغيّر في
الملفين في نفس التعديل، وإلا الواجهة هتمنع اختيار فترة والـ Worker يوافق
عليها (أو العكس).

**مفيش `CACHE_VERSION` bump مطلوب** — شكل الصف نفسه ما اتغيّرش، بس مفاتيح الكاش
لفترات قبل التاريخ ده بقت ببساطة مش هتتطلب تاني (مش خطر، مجرد مفاتيح يتيمة).

---

## هيكل الكود

**`index.js`** — `§CONSTANTS` · `§CORS` · `§HELPERS` · `§SHARED` · `§ACCESS-LOG` ·
`§SHOPIFY` · `§AGGREGATE` · `§CACHE` · `§HANDLER` (`§AUTH` · `§DATA` ·
`§LOG-ENDPOINTS` · `§DIAG`)

**`index.html`** — `§STYLES` · `§SHELL` · `§LOGIN` · `§TOOL` (٦ تابات: نظرة عامة ·
تحت التجهيز · خرج للشحن · المناديب · المناطق · الإلغاء والمرتجعات — سجل الدخول
اتلغى 30-08-2026، والتجهيز والطوابير اتقسم لتابين 30-08-2026، وتاب "المناديب
والمناطق" اتقسم لتابين 30-08-2026) · `§CONFIG` ·
`§SHELL-JS` · `§AUTH-JS` · `§DASH-JS` · `§REASONS-JS` · `§ORDERS-JS` ·
`§PREP-JS` · `§SHIPPED-JS`

### طبقة التحميل الشهرية (`§DASH-JS`)

```
computeMonthBoxes()     → الشهر الحالي + لحد ٥ سابقين، مقصوصة على STORE_START_DATE
selectMonthBox(ym)      → فلترة الشاشة على شهر واحد + تحديث فورس من شوبيفاي (مش كاش)
monthChunks(from, to)   → بتقسّم على حدود الشهور، وبتقصّ أول وآخر شهر على حدود الفترة
forcedChunkIdx(chunks)  → الشهور اللي زرار "تحديث" بيسحبها (المفتوحة، أو الأحدث لو الفترة كلها في الماضي)
probeAndLoad()          → get_meta لكل شهر بالتوازي → كله متكاش؟ حمّل : اعرض الشهور الناقصة بالاسم
loadData({mode})        → 'cache' | 'open' | 'all' — بتمشي شهر ورا شهر بشريط تقدّم
oldestChunkUpdate()     → طزاجة الفترة = أقدم شهر فيها
```

⚠️ **`selectMonthBox` هي الاستثناء الوحيد** في الأداة اللي بتنادي `loadData({mode:'all'})`
مباشرة من غير `probeAndLoad` — أي مصدر تغيير فترة تاني (من/إلى تاريخ) لسه
كاش-أولاً عادي.

⚠️ **الشهور بتتحمّل بالتسلسل مش بالتوازي** — التوازي بيولّع throttle على شوبيفاي
وبيخلي الأخطاء متشابكة، والتسلسل بيدي شريط تقدّم حقيقي.

⚠️ **شهر واحد بيفشل = التحميل كله بيفشل ومفيش أي رقم بيتعرض** — نفس قاعدة
"الصفحة الفاشلة تفشل الطلب كله" بس على مستوى الشهر. الرسالة بتسمّي الشهر
(`مايو 2026 — Shopify رجّعت HTTP 503`).

### التقسيم اللي ماينكسرش

- **الـ Worker بيملك أكواد المربعات والأسباب** (`B` / `R`) ·
  **الـ HTML بيملك أسماءها والإجراءات** (`BUCKET_META` / `UNCL_REASON_LABELS`).
- كود سبب جديد في الـ Worker من غير تسمية في `UNCL_REASON_LABELS` **بيتعرض بنصّه
  الخام** — مقصود، عشان النقص يبان بدل ما يختفي.
- أي حقل جديد في `ORDER_QUERY` → **`CACHE_VERSION` يترفع في نفس التعديل**، وإلا
  الفترات المتكاشة بترجع صفوف من غيره والمربّع الجديد يطلع صفر بلا أي خطأ.

---

## الـ endpoints

```
get_data (POST)   · get_meta (GET)   · clear_cache (POST)
get_access_logs · get_access_logs_count · get_access_logs_export   (GET — لا تاب بيقراهم في الواجهة، اتلغى 30-08-2026)
get_logs        · get_logs_count        · get_logs_export          (GET — قياسية، بترجع فاضية هنا)
check_employee · register_pin · verify_employee · log_logout · get_employees
diag · get_config
```

---

## حدود معروفة — مش أعطال

1. **مفيش أي بيانات قبل `2026-05-01`** — بداية تشغيل المتجر (قرار أحمد
   30-08-2026). سؤال "فين أوردرات مارس/أبريل" متوقّع، مش باج.
2. **Skechers · Columbia · North Face · Ecco** مش على شوبيفاي (WordPress بس) —
   مش موجودين في أي رقم في الأداة. → `woocommerce-sync-helper`.
3. **أوردرات بوسطة مالهاش `pickup_date`** إطلاقًا — الحقل بيتكتب لمناديب
   القاهرة/الجيزة بس. بتظهر تحت "(بدون تاريخ)".
4. **الأعمار محسوبة من `createdAt`** — مش من وقت دخول الحالة. الميتافيلد بيحمل
   الحالة الحالية بس. أي KPI مدة حقيقية لازم ييجي من D1
   (`tool = 'metafields_change'`) — مش منفَّذ هنا.
5. **أوردر بأكتر من دورة R/E**: المربّع بيوصف الدورة **الحالية** بس؛ الدورة
   القديمة مش ظاهرة في أي رقم (`classification-rules.md` §9-ب لسه ما اتبنتش).
   الصف بياخد `MULTI_CYCLE`.
6. **أول تحميل لفترة طويلة بياخد دقايق** — نداء بالتسلسل لكل شهر. بعد كده
   التحديث العادي = شهر واحد. حجم مفتاح الشهر ≈ ١.٥ ميجا (الحد ٢٥) — هامش مريح.
7. **أكتر من ٢٠ سطر منتج** في أوردر → `LINEITEMS_TRUNCATED` + `UNCLASSIFIED`.

---

## اختبارات اتنفّذت وقت البناء (29-08-2026)

- **٢٣ حالة تصنيف** على أمثلة حقيقية من الستور — كلها نجحت، وفيها الحالتين
  المرجعيتين: `#50091` (Rule 15 — استبدال مقفول ثم مرتجع مفتوح → `SHIPPED_RETURN`)
  و`#51496` (دورتين مفتوحتين → `MULTI_CYCLE` + `CYCLE_OVERLAP`).
- **بيانات حقيقية**: ١٢٠ أوردر من ٠١–٠٢/٠٧/٢٠٢٦ (فترة ناضجة) → صفر `UNCLASSIFIED`
  وصفر تحذيرات، ومجموع المربعات = إجمالي الأوردرات.
- **الواجهة** في متصفح فعلي (Chromium): الدخول · مؤشر الطزاجة · المربعات الستة ·
  البانرات · ٦ تابات · الرسوم · **الحالات التلاتة لأيقونة الفلتر** ·
  **الحالات التلاتة للترتيب** · الترقيم · صفر أخطاء console · وتدهور آمن لو
  Chart.js/ExcelJS ما حمّلوش (الأرقام والجداول بتفضل شغالة مع بانر واضح).
- **التحميل الشهري** في متصفح فعلي، ٦ سيناريوهات:
  ① `get_meta` بينادي لكل شهر والشهر الناقص بيتعرض **بالاسم** ·
  ② ٦ نداءات `get_data` بالتسلسل ودمج ٦٠٠ صف ·
  ③ **"تحديث الشهر الحالي" سحب شهر واحد بس من شوبيفاي** (١ من ٦) ·
  ④ "تحديث كل الشهور" سحب الستة ·
  ⑤ فترة شهر واحد في الماضي → اللافتة بتتغير لـ "تحديث أحدث شهر" وزرار "كل
  الشهور" بيختفي · ⑥ فشل شهر → رسالة باسم الشهر و**صفر مربعات ظاهرة**.

> ⚠️ **الاختبار الوحيد اللي ما اتنفّذش:** التحميل الفعلي لست شهور كاملة من
> شوبيفاي عبر الـ Worker المنشور (مفيش Worker منشور وقت البناء). الوقت وحجم
> الرد الحقيقيين لازم يتأكدوا من أول تشغيل.

## اختبارات اتنفّذت وقت تعديل 30-08-2026 (تاريخ بداية البيانات + مربعات الشهور)

- **`node --check index.js`** — الـ Worker سليم تركيبيًا.
- **الواجهة في Chromium فعلي** (بدون Worker منشور — مفيش سيرفر متاح وقت
  التعديل، زي وقت البناء الأول): صفر `pageerror`، شاشة الدخول بتفتح عادي،
  ومربعات الشهور بتتولّد صح — **٤ مربعات بس (مايو → أغسطس ٢٠٢٦)** مش ٦، عشان
  التاريخ النهارده (30-08-2026) لسه قريب من `STORE_START_DATE` — سلوك متوقّع
  ومقصود، مش نقص.
- **الفحص الآلي (Step 9 في `ecommoda-html-builder`)**: z-index (login 500 ·
  كل الـ modals 600 · toast 9999) · `.container` بيلف المحتوى فعليًا ·
  `currentEmployee` مش في localStorage · مفيش `main-seg` قديمة — كله سليم.

> ⚠️ **لسه ما اتعملش:** اختبار فعلي للضغط على مربّع شهر بعد تسجيل دخول حقيقي
> (مفيش Worker منشور ولا بيانات موظفين متاحة وقت التعديل). المنطق (`selectMonthBox`
> → `loadData({mode:'all'})`) اتراجع كود بس مش اتشغّل end-to-end.

---

## بصمة المهارات

| المهارة | الإصدار وقت آخر تعديل |
|---|---|
| ecommoda-worker-builder | v1.0.0 |
| ecommoda-html-builder | v1.1.0 |
| ecommoda-dashboard-builder | v2.0.0 |
| ecommoda-order-lifecycle | v1.1.0 |
| ecommoda-constants | v1.2.0 |

آخر مطابقة: 30-08-2026 · `index.js` v1.1.0 · `index.html` v1.7.0

🔴 معلّقة: — لا شيء

آخر تحديث: 30-08-2026 — 20:15

</div>
